# DECISIONS

Consequential decisions with rationale. Detailed architecture decision records live in
`docs/adr/`. This file is the running index.

| # | Decision | Rationale | ADR |
|---|----------|-----------|-----|
| D-01 | Extend the existing repository rather than rewrite it. The wheel-strategy decision-support dashboard (milestones 1–10, 951 passing tests) is kept intact; the deterministic strategy platform is added as new `chronos.*` subpackages. | The existing code already embodies the required safety posture (hard-disabled orders, fail-closed reconciliation, Decimal money). Rewriting would destroy tested invariants. | ADR-0001 |
| D-02 | Interactive Brokers API access stays on the TWS API via `ib_async`. | Already a dependency; actively maintained community successor to ib_insync; TWS API supports paper accounts, what-if orders, executions, and account events. Client Portal Web API rejected for session-keepalive complexity and weaker order-event semantics for this use case. | ADR-0002 |
| D-03 | The new platform persists to its own SQLite database file (separate from the wheel ledger), append-oriented tables, schema versioned. | The existing schema v2 binds a database file to one account fingerprint with strict adoption rules; mixing platform state into it would weaken those invariants. | ADR-0003 |
| D-04 | Strategy code emits `StrategyProposal` objects only; a separate portfolio layer converts proposals to sized `OrderIntent`s; an independent risk engine validates every intent deny-by-default; only the execution layer talks to a broker adapter. | Required separation of authority; strategies cannot see broker or risk internals. | ADR-0004 |
| D-05 | Event-time simulation and live paths share one deterministic engine loop; all timestamps UTC internally; bars are processed only when closed (confirmed), matching Pine `barstate.isconfirmed` semantics with `calc_on_every_tick=false`. | Reproducibility and Pine parity; eliminates intrabar repainting class of bugs by construction. | ADR-0005 |
| D-06 | Backtest fills model: signals computed on bar close t, orders filled at bar open t+1 with configurable slippage and commission; no same-bar fills. | Conservative, replicable, matches how the daily-bar strategies would actually be traded; avoids look-ahead. | ADR-0005 |
| D-07 | Research data acquired from public dataset mirrors through authenticated connector access, with per-file provenance, hashes, and integrity validation recorded. IBKR historical data is the intended production source once the owner supplies credentials. | Direct market-data vendor endpoints are unreachable from this build environment. | ADR-0006 |
| D-08 | Operating modes RESEARCH/BACKTEST/REPLAY/SHADOW/PAPER/CANARY_LIVE/LIVE implemented as a runtime mode lock with multi-condition activation; CANARY_LIVE and LIVE are implemented as *recognized but refused* states **in this deterministic platform's mode lock** (`chronos.control.modes`), which still raises unconditionally and is untouched by ADR-0016. *(Scoped 2026-07-25: the separate `chronos.orders` plane gained a gated live branch in Milestone 7 — ADR-0009 — so the original phrase "the live submission path raises unconditionally in this codebase version" is no longer true repository-wide.)* | One mistyped variable must not enable live trading; refusing in code beats configuration. | ADR-0007 |
| D-09 | Halt (kill-switch) state persists in the platform database and must be cleared by an explicit operator rearm command; process restart re-enters reconciliation and keeps the halt. | Restart must never clear a risk halt. | ADR-0007 |
| D-10 | Pine corpus fetched from Notion (owner's authoritative store) into `research/pine/`, SHA-256 hashed; audits and registry reference hashes, not page URLs alone. | Reproducibility of the audit against exact sources. | — |
| D-11 | ~~No generative model output feeds any runtime decision. AI was used to author code, audits, and documentation offline; every runtime path is deterministic, versioned, and tested.~~ **SUPERSEDED by D-16 (2026-07-25).** Kept for history; read it as the posture through that date, not as current policy. | Was: non-negotiable rule in the build brief. Superseded by an explicit owner directive re-scoping Chronos as an autonomous, model-driven system. | ADR-0004 §5 (superseded by ADR-0016) |
| D-12 | Initial executable candidates limited to daily-bar, long-only ETF strategies derived from the corpus (Markov regime-gated trend continuation family; RSI-2-class mean-reversion family), validated against simple baselines; intraday corpus scripts classified research-only. | Account size (~USD 3k), PDT constraints, data availability, and cost floor make intraday untradeable for this account; honesty over ambition. | ADR-0008 |
| D-13 | IBKR historical bars are ingested by a separate, read-only data process (`chronos.histdata`, own `ib_data_client_id`) into a file store of **unadjusted** bars plus a corporate-action event stream; adjusted / total-return views are derived at read time and never written back. The process is structurally isolated — it opens no trading database, holds no writer lease, and imports no order/broker module (AST + subprocess import tests forbid the order plane and `sqlalchemy`/`sqlite3`; `ibapi` stays lazy). Succeeds D-07's approximation of following the source's adjusted series. | Retroactive re-adjustment would break hash-pinned provenance; a data process that could place an order or hold the lease would be catastrophic; the real fetch is owner-gated (invariant 8) and built against a fake in CI. | ADR-0011 |
| D-14 | The same read-only data process captures **forward** option-chain / IV / greeks EOD snapshots (`python -m chronos.histdata options`) for allowlisted underlyings into immutable per-day files, over a **bounded** window (expiry horizon + strike band of spot, both recorded in the snapshot). Every row carries its quote's `DataQuality`, and each snapshot records a staleness histogram — $0-tier delayed/EOD data is labeled, never presented as live. No greeks/IV are fabricated (absent ⇒ `None`); the store ships empty; the real fetch is owner-gated. | IBKR keeps **no history for expired options** — there is no backfill at any price, so forward capture must start ASAP; honest staleness labeling and recorded capture bounds keep a delayed/partial snapshot truthful rather than misleading. | ADR-0012 |
| D-15 | Research runs and holdout events are recorded in a tamper-evident, hash-chained registry ledger (`chronos.registry`, record-compatible with `auditlog`) with an out-of-band **head anchor** so truncation/deletion is caught; it is the source of truth for the multiple-testing **trial count** (derived from registered starts/runs) and burned holdout scopes. Consuming a holdout requires an owner-typed, **single-use, file-locked, logged** unlock (module-constant phrase via `hmac.compare_digest`, never a setting/logged), rationed by a data-accrual budget and bound to the immutable window definition, name-independent scope, relevant stored bytes, and full HOLDOUT set. The guardian verifies the ledger before trust, refuses renamed/overlapping burns or grant-time drift, and selectively unmasks only the authorized window. The registry is research-plane (opens no trading DB, imports no order/broker); no shipped scheduled/service/control/execution/orders/runtime path imports or calls the unlock (AST + single-selective-unmask-site tests), model plane barred (written prospectively against `chronos.copilot`; **retargeted by D-16**: `chronos.autonomy` joins the scanned automated tree, so the model plane cannot import the registry or call the unlock — the guarantee ADR-0013 §7 actually makes, preserved. The converse half — forbidding the automated tree from *importing* the model plane — is deliberately not applied to `chronos.autonomy`, because D-16's deterministic supervisor must import the decision contract in order to judge it; it remains in force against the never-built `chronos.copilot`). | The M5 review's "burned holdout" failure — a holdout silently reused as fresh — must be detected and refused, and self-reported trial counts are theater; an anchor-verified ledger plus immutable, once-only scope binding makes both auditable and enforced in code. Honest residuals (both-files rewrite, runtime-reflection evasion, trial-count completeness) are recorded in ADR-0013 §11 / limitations. | ADR-0013 |
| D-21 | **Phase-3 ordinary research uses a canonical start-before-read lifecycle and retained evidence.** `CanonicalTrialRegistry()` owns the fixed ADR-0013 ledger; every brokered attempt writes `trial_started` before its manifest-bound catalog may open bytes, and completion follows only after exact input/output objects plus a replay envelope are immutable in the fixed-root SHA-256 store. Failed, retried, and interrupted starts count. Catalogs reject ordinary/holdout path or content aliases; public metadata exposes no data path. The runner rejects registry/store capabilities from different workspaces before starting, and restart verification binds the canonical terminal to the envelope and every object. Holdouts remain unavailable without the separate guardian. Legacy campaign/walk-forward/generic paths are not silently upgraded and remain non-certified. | The Phase-3 plan requires one global N and byte-replayable evidence. Caller-selected ledgers, callbacks, free-floating results, and unverified completion records cannot prove either. Binding one canonical start to one restricted read and one retained, restart-verifiable envelope creates the narrow enforceable boundary while honestly leaving final-N scoring, certified data, evaluator authorization, and promotion blocked. | ADR-0013 §9 |
| D-22 | **The Five-Tool v2 campaign is compiled from exact typed metadata and is campaign-atomic, but remains deliberately non-executable.** The public compiler accepts the frozen pending seven-cell topology and returns typed blockers with no partial plan; it categorically refuses resolved substitutes until a successor schema binds reviewed per-cell evaluator semantics. The full-stack reference arm is identity-only. Every future trial blueprint must carry both the exact reviewed-manifest SHA and the normalized semantic campaign SHA; neither identity alone authorizes data access or execution. An unexported synthetic compiler seam exists only to test deterministic plan and lifecycle invariants and grants no certified reader, runner, holdout, or promotion capability. | The v1 prose overlays could label arbitrary settings as scientific ablations, silently duplicate equivalent executions, and conceal missing H3/H4/H6 stream semantics. Exact schemas, complete effective-setting diffs, preregistered neighbor axes, unique content/config identities, and all-or-nothing plan suppression make unresolved science explicit without spending trial multiplicity or touching data. | — |
| D-16 | **An approved generative model may originate runtime trading decisions only through a typed AITradeDecision and the single deterministic ModelDecisionGateway. The model cannot access IBKR directly, change its authorization, weaken policy, or bypass any deterministic gate.** Trade-time authority is granted by an owner-authored, versioned, expiring, revocable AutonomyMandate; policy-time control (objectives, model/provider versions, permitted instruments and strategies, capital and risk allocation, promotion, activation/revocation, kill switch) stays with the owner. The deterministic kernel keeps unconditional veto authority and every guarantee in ADR-0016 §8 is unweakened. Supersedes D-11. | Owner directive (2026-07-25) re-scoping Chronos as a fully autonomous, model-driven trading system. The hazard D-11 addressed by prohibition is now addressed by structure: one decision type, one gateway, one transmit site, an expiring owner mandate, and an unconditional deterministic veto. | ADR-0016 |
| D-19 | **The operator terminal's chart reads bars from the broker, not from the research corpus, and paces itself by degrading rather than waiting.** `Broker.historical_bars` joins the protocol (closed bars only; `official_ibkr` implements it, `demo` emits a deterministic synthetic series that the panel banner-labels, `ib_async` refuses and points at the official adapter). The obvious cheap option — serving `chronos.histdata`'s store — was rejected on the data: SPY ends 2019-11-14, IWM covers 2019–2021, and R-08 records the corpus is heterogeneous (some adjusted, some nominal, some transcribed to 2dp). That is backtest material and useless for supervising a live trader, and reading it would have dragged ADR-0013's holdout question into a display surface. `chronos.api.bars.BarProvider` caches by `(symbol, interval)` remembering its coverage depth, **never sleeps** on a pacing delay (serves cache labelled stale, or refuses), records budget **before** the call so a failing symbol cannot retry unthrottled, and holds no lock across a broker call. `PacingController` moved to `chronos.marketdata` so both self-pacing callers share one rule without the API process importing the research plane. Panel dedupe became `(panel, symbol)`. | The dominant risk is that a chart panel becomes an unbounded request source on the one connection the order pipeline and autonomy tick share, against IBKR's most rate-limited surface. Caching is a safety property here wearing a performance costume; blocking on pacing would put a chart in front of an order submission on the same event loop. Disclosed residual: the budget is per-process and IBKR's may not be (ADR-0011 §5, now with two callers). | ADR-0019 |
| D-18 | **The operator terminal is built fresh inside Chronos, served by FastAPI, with no Node toolchain and no second runtime.** Two existing owner-built terminals were evaluated as candidates and both rejected on evidence: adopting **tyche** without forking is structurally impossible (UI modules are build-time only, and its `DataProvider` plugin plane has no `portfolio` method, so positions/orders/account — the whole point — cannot ride the sanctioned extension point; it also declares itself "not a broker, no order-placement path"), and forking **midas** buys a ~128-board crypto catalog that is dead weight for IBKR equities at a ~USD 110 account, a parser that mangles IBKR option/future symbols, and an AGPL-3.0-only license, while still leaving the trading surface to be written from scratch (its order routes are a hardcoded 503). The decisive fact: **the dominant cost is Chronos-side and identical under every option** — no routes exist for tick health, queue depth, cycle journal, session counters, or mandate state; no streaming, no CORS, no static serving. A TS shell removes none of that work and adds a second language, lockfile, and supervised process beside the one that moves money. **The inversion that makes fresh better than merely cheaper: the command registry and every panel read-model live in Python** (`chronos.terminal`) and are served as JSON to a build-free ES-module client, so the terminal's contract is covered by the gates that already protect this repo (mypy strict, ruff, pytest), the dependency surface does not grow, and same-origin serving dissolves the browser-token problem with no BFF. **Execution posture resolved:** the terminal is display + owner-actions (arm/disarm, kill/disengage, acknowledge, revoke) and reaches execution only through the *existing, unchanged* order routes — ADR-0016's "terminal ships with execution still disabled" was M1-era sequencing spent by ADR-0017, while E2's "byte-identical to manual flow" is the constraint that actually binds. Design taken from tyche (Apache-2.0, attributed in NOTICE); midas influenced ideas only, no code. | Seven-agent reconnaissance read all three codebases before the call. Adopting a shell would have meant paying the full backend cost anyway, then maintaining a second runtime to display it, then writing the trading surface regardless — while giving up the ability to test the terminal's contract under the repo's existing strict gates. | ADR-0018 |
| D-20 | **Reconciliation readiness is re-established on a bounded periodic cadence, and expires on age.** A writer-only lifespan task re-runs `reconcile_submission_readiness()` every **120 s** while positioned in RTH, **240 s** flat in RTH, and **1800 s** while the market is closed; readiness older than the **300 s** maximum evidence age reads as `PENDING` regardless of the loop's health, and readiness never crosses a regular-session open. Closes plan §6 finding 1. | Today the first opening order consumes readiness (`submission_guard`) and nothing re-arms it, so every later opening order is blocked until restart. Re-arming it is **widening in effect** even though it restores intended function, so it is owner-gated and recorded rather than slipped in. The cadence is set by what can change the book without an order this system placed — a fill on a resting limit, and overnight assignment — and bounded on the other side by the shared 6-per-window pacing budget, where headroom is a safety property because rate limit spent watching is rate limit unavailable to cancel. Two consecutive failed cycles fail closed by arithmetic rather than by error handling. | ADR-0020 |
| D-21 | **The owner builds, Chronos trades — live experimentation authorized at owner-capped size.** Owner directive (2026-08-09): "I'm willing to risk my own capital to see if this AI algo trading bot can trade like me or better. I want to outsource my day-trading tasks to AI. This is my hobby: I build and improve Chronos, while Chronos trades and implements my investment strategies." The promotion ladder is re-scoped to gate **claims** ("validated", "proven", "better than the owner"), which keep the frozen evidence bar unchanged; owner-authorized live trading at bounded size instead waits on the ADR-0025 go gate (funded capital + typed loss limits, read-only gateway campaign, ≥20 paper lifecycles / ≥10 sessions, TradingView parity export, market-data subscription, owner-authored mandate + kill drill — frozen before observation). Finding 4 resolves toward standing mandate authority (arming retirement is follow-up, owner-reviewed work; code wins until it lands). Rungs above SHADOW stay owner-declared per the ADR-0024 interim rule — D-21 records the intent, the owner types the mandate. Every execution-correctness mechanism (single transmit site, ten-gate stack, ADR-0009 conjunction, kill precedence, deny-by-default mandates, model isolation, registry/replay discipline) is explicitly unweakened. Live P&L at small N is anecdote, recorded as such — capital risk buys experience, not evidence. | The owner's stated relationship to the project: a hobby whose purpose is delegated trading. Forbidding owner-capped live experimentation until a 24-36-month ladder completes serves no one the safety machinery protects; laundering that experimentation into evidence would corrupt the discipline that makes the experiment informative. This row separates the two cleanly, the same move D-16/D-17 made for autonomy authority. | ADR-0025 |
| D-17 | **Maximal autonomy, owner-directed.** A persistent mandate file (`AUTONOMY_MANDATE_FILE`) auto-activates on every boot — a running backend plus a valid, account-matching mandate is enough to trade, with no per-boot ritual (supersedes ADR-0016 §4 "an env var alone may not activate live"). The live-mandate ceiling moves 30d→365d and `restart_behavior` defaults to `RESUME_UNTIL_EXPIRY`. A `model_discretion` grant inverts deny-by-default **for capital ceilings only**: an unset ceiling means "affordability is the bound," while any ceiling the owner *did* set still binds. `OrderForm.MARKET` exists but compiles to a **protected** collared limit (quote±1%), never an unbounded venue `MKT`; the compiler prefers the most-aggressive *granted* form. **Not superseded:** floors/reserve, single transmit site, writer lease, kill switch, reconciliation, stale-data refusal, the full propose→preview→confirm→submit handoff, the degraded-state rule, and deny-by-default for everything the owner did not grant discretion over. Revocation survives restart; an invalid/wrong-account file boots inert with a CRITICAL alert. Closes the R-36 wiring residual. | Owner directive (2026-07-25): Chronos "as close to autonomous as possible," modeled on the reference Quant-Guild bot. "Maximal" was scoped as removing friction and owner-optional ceilings, not execution-correctness mechanisms; market orders implemented as protected rather than unbounded, with the literal-unbounded interpretation flagged as a separate un-taken decision. | ADR-0017 |
| D-22 | **A TradingView alert may become a Chronos proposal, through a separate unprivileged bridge process that grants nothing.** `chronos.bridge` listens for a TradingView webhook, authenticates it against an owner-configured shared secret, translates it into candidate proposal JSON, and POSTs that to the *existing* loopback `/autonomy/proposals` with the *existing* local API token. It is a client of the ingress, not a new door into it: the route, the parser, and every downstream gate are unchanged, so a TradingView-sourced proposal walks the same fifteen admission checks, the same sizing against mandate ceilings and floors, the same capability-matrix compiler, and the same propose→preview→confirm→submit handoff. The load-bearing choice is that the bridge **does not import `chronos.autonomy`** — it emits a dict and the ingress decides whether that dict is a proposal, so there stays exactly one place where "what a valid decision is" is decided. The resulting duplication is guarded structurally: an isolation test pins every restated enum *equal* to the real one, and an exercised test pushes the bridge's own output through the real `ingress.parse_proposal` per decision kind. Ships inert — `CHRONOS_TV_BRIDGE_FORWARD` defaults false, both allowlists are required, the secret is mandatory, and a non-loopback ingress URL refuses to boot. Equities only; the four ADR-0021 inert fields are deliberately never populated. **Not superseded:** ADR-0016 §8 and ADR-0017 §5 in full; the mandate remains the only grant of trade-time authority and the bridge cannot read, name, write, or activate one. An alert is not evidence and creates no promotion rung. | Owner directive (2026-08-12) following D-21: the owner's strategies already take the form of TradingView alerts, and a webhook is the only export TradingView offers. Adding a network channel is owner-gated + new-ADR work, so the mechanism is built inert and the three live decisions (publishing the listener, enabling forwarding, and whether alerts are an appropriate source for this account) stay with the owner. Disclosed residual, sharpened rather than closed by this row: provenance still cannot distinguish a TradingView-sourced proposal from a model-worker one, because both are stamped with the static `INGRESS_IDENTITY` — the bridge compensates with a digest-bearing evidence citation, and plan §6 finding 6 / ADR-0023 stay open. | ADR-0026 |
| D-23 | **The model worker exists: an in-repo, out-of-process AI brain that proposes and only proposes.** `worker/` (top-level, deliberately NOT part of the `chronos` package or wheel; run as `python -m worker`) implements the external worker ADR-0016 §3 designed the autonomy stack around: each cycle it reads the backend's token-protected read endpoints into a digested canonical-JSON evidence snapshot, sends it to Claude with the owner's editable trading policy (`worker/policy.md`), forces one `propose_decision` tool call (`strict: true`, `additionalProperties: false` — provenance, decision_id, accounts, prices, routing and transmit are unrepresentable in the schema, not filtered from it), validates the result as hostile input against a restated-and-pinned vocabulary, stamps an evidence citation whose digest is the SHA-256 of the exact bytes the model saw, and POSTs the candidate to the existing loopback ingress. Three structural choices carry it: the worker imports NOTHING from `chronos` (AST + subprocess probe; converse also pinned — nothing in `src/chronos` imports it); no LLM SDK enters the repo's dependency tree (raw httpx to the Messages API; the old manual grep is now a failing test); and the model's only output channel is the forced tool, so ADR-0016 §5's "prose is never parsed into orders" is the transport, not a discipline. Ships inert: `CHRONOS_WORKER_FORWARD` defaults false, watchlist and kind allowlists required, loopback-only backend URL, evidence gathered or the cycle refuses to think, model refusal/truncation/prose → no proposal. **Not superseded:** every gate; the mandate remains the only authority and a worker with no mandate produces judged refusals, not trades. A worker decision is not evidence and touches no registry or rung. | Owner directive (2026-08-12), completing D-16/D-21: the fortress was built around an empty throne — every control on the proposal path existed and no model had ever proposed. Building the worker in-repo (but out-of-package, out-of-process) keeps it testable under the repo's gates while preserving the ADR-0016 §3 process boundary and the no-SDK/no-key invariants of the broker-holding process, verbatim. Residuals sharpened, not closed: static `INGRESS_IDENTITY` now covers three indistinguishable sources (worker, TradingView bridge, any token-holder) — plan §6 finding 6 / ADR-0023 is overdue; the policy file is unpinned strategy authority pending the same protocol; R-30 prompt injection stays bounded-not-solved with the snapshot as the new surface. | ADR-0027 |
| D-24 | **Proposal authorship is a registered, revocable credential — never a shared token, never the payload's claim.** ADR-0023 Option A: `AUTONOMY_PROPOSERS_FILE` names an owner-authored registry (`chronos.supervisor.proposers`) binding each proposer's **credential hash** (SHA-256 only — the file cannot yield a credential) to the exact identity facts the queue writer stamps: provider, model, model/prompt/tool/decision-schema/policy versions, a hard expiry, and an enable flag. Unset, nothing changes: proposals authenticate with the local API token and carry the static ingress identity, exactly the pre-registry posture. Set, the posture flips whole: `POST /autonomy/proposals` requires a registered `X-Chronos-Proposer-Token` (the general token is refused there, with a message that says why), the route records the *verified* proposer_id on the durable queue row, and the drain re-resolves it into a `HarnessIdentity` **at the moment authority is exercised** — a registration that expired between enqueue and drain refuses at STAMP (`PROPOSER_UNRESOLVED`), and a configured-but-broken registry refuses every proposal (route 503 + STAMP) with a CRITICAL alert rather than falling back to anonymous proposing. The registry itself is a boot-time snapshot, exactly like the mandate file: disabling or deleting a registration takes effect at the next backend restart, expiry is the transition the snapshot carries live, and ~~the mid-session stand-downs remain the kill switch, mandate revocation, and a restart.~~ **[Corrected 2026-08-14 by D-26/A3: a single leaked credential is now stood down live by `chronos.cli proposer revoke`, a durable hash-chained act both planes consult per check — no restart. The rest of the sentence stands: enabling and re-registering are still restart-scoped, deliberately.]** The credential is proposal-only in both directions, exercised route-by-route over the app's own table; provenance and the hash-chained proposal journal now carry `proposer_id`; and the placeholder evidence digest became honestly absent (`None`) instead of sixty-four zeros, with admission's check 9 refusing a digest disagreement in *every* direction (claimed-when-absent included). `chronos.cli proposer mint|check` are stdout-only — a tool that mints text is not a tool that grants. Building the exercised proofs also surfaced and fixed a live inert-control defect: the proposal route's `_fingerprint_of` read an attribute `AppRuntime` never had, so **every** real proposal POST had answered 503 `BACKEND_UNSCOPED` (and `GET /autonomy/alerts` answered `[]`) since M7 — fail-closed in direction, dead-ingress in effect. **Not superseded:** the mandate remains the only grant of trade-time authority; registration is identity, not authority — a registered proposer can do exactly one thing an anonymous caller cannot, which is be *attributed*. | Owner directive (2026-08-12): "lets build the worker identity protocol next" — Option A built directly, delivering ADR-0023's recommended Option B (the credential split) as a subset. Closes plan §6 finding 6: provenance now authenticates an author, not the ingress, and the mandate's version pins finally bind a grant to a registered authorship — a mandate pinned to the worker's registration refuses the bridge's proposals at admission check 8. Disclosed bounds: authorship pins by version *tuple*, not by name (two registrations sharing identical version fields are indistinguishable to admission; `mandate check` lists every match); evidence binding stays uniform (registrations carry no evidence fields; the job/evidence protocol is future ADR work); identity resolution trusts the owner-secured registry file the way the mandate loader trusts the mandate file. | ADR-0023 |
| D-25 | **Evidence binding is a per-job record the backend issues, and admission check 9 finally has two sides.** ADR-0028 Option C. The finding that justified building it: check 9 compared `provenance.evidence_bundle_id`/`_digest` against `SupervisorState.expected_*` and **both sides were two reads of the single `INGRESS_IDENTITY` constant** — one copy stamped by the queue writer, the other copied into `CycleFacts` by the backend gatherer — so the check could not refuse, in any posture, for any proposer. Written correctly, wired to a comparison that had never had two independent origins: the R-24..R-27 shape one level up. `AUTONOMY_EVIDENCE_BUNDLES` unset changes nothing (proven byte-identical to `b5d61dd` against a recorded journal row, not by inspection). Set, the posture flips whole: `POST /autonomy/evidence` issues a durable, hash-chained bundle record (`autonomy_evidence_bundles`, migration 0008, schema v9) to the *presenting credential* — either `backend_served`, where the backend composes a canonical document and digests **the exact bytes it serves**, or `alert_attested`, where a proposer asserts a digest of bytes Chronos never saw. The drain resolves the cited bundle against that record **at its own clock**, so unissued, foreign-proposer, and expired-between-enqueue-and-drain all refuse at STAMP before the proposal is judged, and provenance is stamped from the RECORD. Check 9 then gains the independent side it never had: the payload's own `EvidenceCitation` — authored by the proposer, never written by the backend — must name the expected bundle, carry a citation kind that record's kind admits, and match its digest. Three additive refusal codes (`EVIDENCE_BUNDLE_EXPIRED`, `EVIDENCE_BUNDLE_UNCITED`, `EVIDENCE_BUNDLE_KIND_MISMATCH`); nothing existing weakens. Evidence binding without a proposer registry refuses every proposal with a CRITICAL alert and never falls back — a bundle is issued *to* a credential. Building the exercised proofs also surfaced and fixed a second live dead-route defect: `chronos.api.bars.provider_for` cached by assigning a new attribute to the `slots=True` `BackendState`, so **`GET /terminal/bars` answered 500 for every symbol, on every backend, since the route existed** — no test had ever called it. **Not superseded:** the mandate remains the only grant of trade-time authority; a bundle authorizes nothing and every gate downstream is unchanged; registration stays identity, not authority. | Owner directive (2026-08-14): "merged 69, go with option C, have opus build it" — Option C chosen over A, B and D because it is the only one under which check 9 stops being a tautology (A and B each move the constant to a per-job value without ever making the comparison meaningful). Closes the evidence half of plan §6 finding 6; the identity half closed 2026-08-12 as D-24. Disclosed bounds, all carried in R-50: **equality catches accident, not malice** — a proposer that fetches a bundle, reasons on other text and cites the issued digest is undetectable, because the backend cannot observe a prompt in another process; **attested is not witnessed**, so an attested bundle may back a proposal and never a promotion rung (ADR-0024's to adopt or reject); a bundle binds which facts were *served*, never that they were true — every fact still comes from a fixture-verified adapter path and nothing here leaves MITIGATED; requiring digest equality couples the backend's serialization to the proposer's rendering, version-pinned via `bundle_version` and fail-closed when they drift; the 300 s TTL and its 3600 s ceiling are disclosed judgments, not derived numbers; and issuance widens a proposal-only credential to a second route, which R-48's per-route enumeration now carries as its one named, tested exception rather than absorbing. | ADR-0028 |
| D-26 | **A leaked proposer credential is revoked by a durable act both planes honor live — keyed on the credential, not on the proposer's name.** `chronos.cli proposer revoke` writes one row in `autonomy_proposer_revocations` (migration 0009, schema v10) and one hash-chained record in the un-scoped `autonomy.proposers` stream; `require_proposer` consults it on every route authentication (401) and the drain-time identity resolver on every STAMP (`PROPOSER_REVOKED`, a new code so the journal can tell a credential the owner killed from a registration that merely aged out). Nothing is cached. Revocation is permanent for that credential and survives restart; there is no un-revoke. | R-48 residual (c) disclosed that the registry is a boot-time snapshot on both planes, so standing down a leaked credential meant editing the owner's file and **restarting the process that holds the broker connection** — during the incident, which is when an operator least wants to bounce it. The keying choice is the operational one: keyed by `proposer_id` a revocation would burn the *name* forever and force the owner to invent `claude-worker-2` mid-incident; keyed by `secret_sha256` it kills exactly the secret that escaped, so re-minting a fresh credential for the same proposer works at the next restart while the leaked one stays dead permanently. The row records the id alongside for legibility. The read is durable and per-check rather than another snapshot — that is the entire point — and fail-closed: an unreadable ledger refuses (503 at the route) rather than admitting. The registry file itself deliberately stays a boot-time grant for everything else; revocation is the one act whose correct latency is *now*. Deliberately not owner-gated to build: it only ever removes authority. | — |
| D-27 | **A registration's `prompt_version` is derived from the policy file's bytes, not typed by hand — and that binds the file, never the run.** `proposer mint --policy-file` sets `prompt_version = sha256(policy bytes)[:16]`; `proposer fingerprint --policy-file` prints the same derivation so a mandate can be re-pinned without rotating a credential; `proposer check --policy-file` reports per registration whether the policy on this machine still matches what was registered. Pinning is opt-in: mint without the flag and the prior typed-label behavior is unchanged. Passing both `--policy-file` and `--prompt-version` is refused rather than resolved by precedence. | R-47 residual (b): `worker/policy.md` is the strategy persona, and nothing bound it to the label a mandate pins against — so editing the policy left the mandate's `VersionPins` still agreeing, and two runs under materially different instructions were indistinguishable in the journal. Deriving the label from the bytes means an edited policy stops matching and admission's existing check 8 refuses `VERSION_PIN_MISMATCH`; no new refusal was added, an existing one stopped being fooled. **The bound, stated because this is the decision most likely to be over-read:** it does not prove which policy the worker *ran*. The worker reads `CHRONOS_WORKER_POLICY_FILE` at startup and nothing in Chronos observes that read, so the digest binds the file as it was at mint time. `check --policy-file` narrows this to drift detection on one machine — the strongest claim available without the worker attesting its own digest, which is a worker change and deliberately not this item. | — |
