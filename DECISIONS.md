# DECISIONS

Consequential decisions with rationale. Detailed architecture decision records live in
`docs/adr/`. This file is the running index.

Next ID = max existing + 1 — scan in the same PR that claims it, never from memory:
`grep -oE '^\| D-[0-9]+' DECISIONS.md | grep -oE '[0-9]+' | sort -n | tail -1`. Duplicate
D-21/D-22 rows exist, annotated in place below; the scan tolerates their (a)/(b) suffixes.

| # | Decision | Rationale | ADR |
|---|----------|-----------|-----|
| D-01 | Extend the existing repository rather than rewrite it. The wheel-strategy decision-support dashboard (milestones 1–10, 951 passing tests) is kept intact; the deterministic strategy platform is added as new `chronos.*` subpackages. | The existing code already embodies the required safety posture (hard-disabled orders, fail-closed reconciliation, Decimal money). Rewriting would destroy tested invariants. | ADR-0001 |
| D-02 | Interactive Brokers API access stays on the TWS API via `ib_async`. | Already a dependency; actively maintained community successor to ib_insync; TWS API supports paper accounts, what-if orders, executions, and account events. Client Portal Web API rejected for session-keepalive complexity and weaker order-event semantics for this use case. | ADR-0002 |
| D-03 | The new platform persists to its own SQLite database file (separate from the wheel ledger), append-oriented tables, schema versioned. | The existing schema v2 binds a database file to one account fingerprint with strict adoption rules; mixing platform state into it would weaken those invariants. | ADR-0003 |
| D-04 | Strategy code emits `StrategyProposal` objects only; a separate portfolio layer converts proposals to sized `OrderIntent`s; an independent risk engine validates every intent deny-by-default; only the execution layer talks to a broker adapter. | Required separation of authority; strategies cannot see broker or risk internals. | ADR-0004 |
| D-05 | Event-time simulation and live paths share one deterministic engine loop; all timestamps UTC internally; bars are processed only when closed (confirmed), matching Pine `barstate.isconfirmed` semantics with `calc_on_every_tick=false`. | Reproducibility and Pine parity; eliminates intrabar repainting class of bugs by construction. | ADR-0005 |
| D-06 | Backtest fills model: signals computed on bar close t, orders filled at bar open t+1 with configurable slippage and commission; no same-bar fills. | Conservative, replicable, matches how the daily-bar strategies would actually be traded; avoids look-ahead. | ADR-0005 |
| D-07 | Research data acquired from public dataset mirrors through authenticated connector access, with per-file provenance, hashes, and integrity validation recorded. IBKR historical data is the intended production source once the owner supplies credentials. | Direct market-data vendor endpoints are unreachable from this build environment. | ADR-0006 |
| D-08 | Operating modes RESEARCH/BACKTEST/REPLAY/SHADOW/PAPER/CANARY_LIVE/LIVE implemented as a runtime mode lock with multi-condition activation; CANARY_LIVE and LIVE are implemented as *recognized but refused* states **in this deterministic platform's mode lock** (`chronos.control.modes`), which still raises unconditionally and is untouched by ADR-0016. *(Scoped 2026-07-25: the separate `chronos.orders` plane gained a gated live branch in Milestone 7 — ADR-0009 — so the original phrase "the live submission path raises unconditionally in this codebase version" is no longer true repository-wide.)* | One mistyped variable must not enable live trading; refusing in code beats configuration. | ADR-0007 |
| D-09 | Halt (kill-switch) state persists in the platform database and must be cleared by an explicit operator rearm command; process restart re-enters reconciliation and keeps the halt. | Restart must never clear a risk halt. | ADR-0007 |
| D-10 | Pine corpus fetched from Notion (owner's authoritative store) into `research/pine/`, SHA-256 hashed; audits and registry reference hashes, not page URLs alone. | Reproducibility of the audit against exact sources. | — |
| D-11 | ~~No generative model output feeds any runtime decision. AI was used to author code, audits, and documentation offline; every runtime path is deterministic, versioned, and tested.~~ **SUPERSEDED by D-16 (2026-07-25).** Kept for history; read it as the posture through that date, not as current policy. | Was: non-negotiable rule in the build brief. Superseded by an explicit owner directive re-scoping Chronos as an autonomous, model-driven system. | ADR-0004 §5 (superseded by ADR-0016) |
| D-12 | Initial executable candidates limited to daily-bar, long-only ETF strategies derived from the corpus (Markov regime-gated trend continuation family; RSI-2-class mean-reversion family), validated against simple baselines; intraday corpus scripts classified research-only. | Account size (~USD 3k), PDT constraints, data availability, and cost floor make intraday untradeable for this account; honesty over ambition. | ADR-0008 |
| D-13 | IBKR historical bars are ingested by a separate, read-only data process (`chronos.histdata`, own `ib_data_client_id`) into a file store of **unadjusted** bars plus a corporate-action event stream; adjusted / total-return views are derived at read time and never written back. The process is structurally isolated — it opens no trading database, holds no writer lease, and imports no order/broker module (AST + subprocess import tests forbid the order plane and `sqlalchemy`/`sqlite3`; `ibapi` stays lazy). Succeeds D-07's approximation of following the source's adjusted series. | Retroactive re-adjustment would break hash-pinned provenance; a data process that could place an order or hold the lease would be catastrophic; the real fetch is owner-gated (invariant 8) and built against a fake in CI. | ADR-0011 |
| D-14 | The same read-only data process captures **forward** option-chain / IV / greeks EOD snapshots (`python -m chronos.histdata options`) for allowlisted underlyings into immutable per-day files, over a **bounded** window (expiry horizon + strike band of spot, both recorded in the snapshot). Every row carries its quote's `DataQuality`, and each snapshot records a staleness histogram — $0-tier delayed/EOD data is labeled, never presented as live. No greeks/IV are fabricated (absent ⇒ `None`); the store ships empty; the real fetch is owner-gated. | IBKR keeps **no history for expired options** — there is no backfill at any price, so forward capture must start ASAP; honest staleness labeling and recorded capture bounds keep a delayed/partial snapshot truthful rather than misleading. | ADR-0012 |
| D-15 | Research runs and holdout events are recorded in a tamper-evident, hash-chained registry ledger (`chronos.registry`, record-compatible with `auditlog`) with an out-of-band **head anchor** so truncation/deletion is caught; it is the source of truth for the multiple-testing **trial count** (derived from registered starts/runs) and burned holdout scopes. Consuming a holdout requires an owner-typed, **single-use, file-locked, logged** unlock (module-constant phrase via `hmac.compare_digest`, never a setting/logged), rationed by a data-accrual budget and bound to the immutable window definition, name-independent scope, relevant stored bytes, and full HOLDOUT set. The guardian verifies the ledger before trust, refuses renamed/overlapping burns or grant-time drift, and selectively unmasks only the authorized window. The registry is research-plane (opens no trading DB, imports no order/broker); no shipped scheduled/service/control/execution/orders/runtime path imports or calls the unlock (AST + single-selective-unmask-site tests), model plane barred (written prospectively against `chronos.copilot`; **retargeted by D-16**: `chronos.autonomy` joins the scanned automated tree, so the model plane cannot import the registry or call the unlock — the guarantee ADR-0013 §7 actually makes, preserved. The converse half — forbidding the automated tree from *importing* the model plane — is deliberately not applied to `chronos.autonomy`, because D-16's deterministic supervisor must import the decision contract in order to judge it; it remains in force against the never-built `chronos.copilot`). | The M5 review's "burned holdout" failure — a holdout silently reused as fresh — must be detected and refused, and self-reported trial counts are theater; an anchor-verified ledger plus immutable, once-only scope binding makes both auditable and enforced in code. Honest residuals (both-files rewrite, runtime-reflection evasion, trial-count completeness) are recorded in ADR-0013 §11 / limitations. | ADR-0013 |
| D-21(a) | **[Annotated 2026-08-22: duplicate number, never renumbered.** Two concurrent lanes each scanned the same max ID and claimed D-21 (2026-08); both merged. This row is D-21(a); the ADR-0025 row later in this table is D-21(b). Renumbering would silently retarget every existing citation — "ADR-0025/D-21" (ADR-0024, ADR-0026, ADR-0027, the vision plan's 2026-08-09 amendment) means (b); "ADR-0013 §9" means this row. Disambiguate by ADR.**]** **Phase-3 ordinary research uses a canonical start-before-read lifecycle and retained evidence.** `CanonicalTrialRegistry()` owns the fixed ADR-0013 ledger; every brokered attempt writes `trial_started` before its manifest-bound catalog may open bytes, and completion follows only after exact input/output objects plus a replay envelope are immutable in the fixed-root SHA-256 store. Failed, retried, and interrupted starts count. Catalogs reject ordinary/holdout path or content aliases; public metadata exposes no data path. The runner rejects registry/store capabilities from different workspaces before starting, and restart verification binds the canonical terminal to the envelope and every object. Holdouts remain unavailable without the separate guardian. Legacy campaign/walk-forward/generic paths are not silently upgraded and remain non-certified. | The Phase-3 plan requires one global N and byte-replayable evidence. Caller-selected ledgers, callbacks, free-floating results, and unverified completion records cannot prove either. Binding one canonical start to one restricted read and one retained, restart-verifiable envelope creates the narrow enforceable boundary while honestly leaving final-N scoring, certified data, evaluator authorization, and promotion blocked. | ADR-0013 §9 |
| D-22(a) | **[Annotated 2026-08-22: duplicate number, never renumbered.** Same collision class as D-21 — concurrent lanes, 2026-08. This row is D-22(a); the ADR-0026 TradingView-bridge row later in this table is D-22(b), and citations of "ADR-0026 / D-22" (docs/tradingview_bridge.md, R-45, docs/limitations.md, the continuation plans) mean (b).**]** **The Five-Tool v2 campaign is compiled from exact typed metadata and is campaign-atomic, but remains deliberately non-executable.** The public compiler accepts the frozen pending seven-cell topology and returns typed blockers with no partial plan; it categorically refuses resolved substitutes until a successor schema binds reviewed per-cell evaluator semantics. The full-stack reference arm is identity-only. Every future trial blueprint must carry both the exact reviewed-manifest SHA and the normalized semantic campaign SHA; neither identity alone authorizes data access or execution. An unexported synthetic compiler seam exists only to test deterministic plan and lifecycle invariants and grants no certified reader, runner, holdout, or promotion capability. | The v1 prose overlays could label arbitrary settings as scientific ablations, silently duplicate equivalent executions, and conceal missing H3/H4/H6 stream semantics. Exact schemas, complete effective-setting diffs, preregistered neighbor axes, unique content/config identities, and all-or-nothing plan suppression make unresolved science explicit without spending trial multiplicity or touching data. | — |
| D-16 | **An approved generative model may originate runtime trading decisions only through a typed AITradeDecision and the single deterministic ModelDecisionGateway. The model cannot access IBKR directly, change its authorization, weaken policy, or bypass any deterministic gate.** Trade-time authority is granted by an owner-authored, versioned, expiring, revocable AutonomyMandate; policy-time control (objectives, model/provider versions, permitted instruments and strategies, capital and risk allocation, promotion, activation/revocation, kill switch) stays with the owner. The deterministic kernel keeps unconditional veto authority and every guarantee in ADR-0016 §8 is unweakened. Supersedes D-11. | Owner directive (2026-07-25) re-scoping Chronos as a fully autonomous, model-driven trading system. The hazard D-11 addressed by prohibition is now addressed by structure: one decision type, one gateway, one transmit site, an expiring owner mandate, and an unconditional deterministic veto. | ADR-0016 |
| D-19 | **The operator terminal's chart reads bars from the broker, not from the research corpus, and paces itself by degrading rather than waiting.** `Broker.historical_bars` joins the protocol (closed bars only; `official_ibkr` implements it, `demo` emits a deterministic synthetic series that the panel banner-labels, `ib_async` refuses and points at the official adapter). The obvious cheap option — serving `chronos.histdata`'s store — was rejected on the data: SPY ends 2019-11-14, IWM covers 2019–2021, and R-08 records the corpus is heterogeneous (some adjusted, some nominal, some transcribed to 2dp). That is backtest material and useless for supervising a live trader, and reading it would have dragged ADR-0013's holdout question into a display surface. `chronos.api.bars.BarProvider` caches by `(symbol, interval)` remembering its coverage depth, **never sleeps** on a pacing delay (serves cache labelled stale, or refuses), records budget **before** the call so a failing symbol cannot retry unthrottled, and holds no lock across a broker call. `PacingController` moved to `chronos.marketdata` so both self-pacing callers share one rule without the API process importing the research plane. Panel dedupe became `(panel, symbol)`. | The dominant risk is that a chart panel becomes an unbounded request source on the one connection the order pipeline and autonomy tick share, against IBKR's most rate-limited surface. Caching is a safety property here wearing a performance costume; blocking on pacing would put a chart in front of an order submission on the same event loop. Disclosed residual: the budget is per-process and IBKR's may not be (ADR-0011 §5, now with two callers). | ADR-0019 |
| D-18 | **The operator terminal is built fresh inside Chronos, served by FastAPI, with no Node toolchain and no second runtime.** Two existing owner-built terminals were evaluated as candidates and both rejected on evidence: adopting **tyche** without forking is structurally impossible (UI modules are build-time only, and its `DataProvider` plugin plane has no `portfolio` method, so positions/orders/account — the whole point — cannot ride the sanctioned extension point; it also declares itself "not a broker, no order-placement path"), and forking **midas** buys a ~128-board crypto catalog that is dead weight for IBKR equities at a ~USD 110 account, a parser that mangles IBKR option/future symbols, and an AGPL-3.0-only license, while still leaving the trading surface to be written from scratch (its order routes are a hardcoded 503). The decisive fact: **the dominant cost is Chronos-side and identical under every option** — no routes exist for tick health, queue depth, cycle journal, session counters, or mandate state; no streaming, no CORS, no static serving. A TS shell removes none of that work and adds a second language, lockfile, and supervised process beside the one that moves money. **The inversion that makes fresh better than merely cheaper: the command registry and every panel read-model live in Python** (`chronos.terminal`) and are served as JSON to a build-free ES-module client, so the terminal's contract is covered by the gates that already protect this repo (mypy strict, ruff, pytest), the dependency surface does not grow, and same-origin serving dissolves the browser-token problem with no BFF. **Execution posture resolved:** the terminal is display + owner-actions (arm/disarm, kill/disengage, acknowledge, revoke) and reaches execution only through the *existing, unchanged* order routes — ADR-0016's "terminal ships with execution still disabled" was M1-era sequencing spent by ADR-0017, while E2's "byte-identical to manual flow" is the constraint that actually binds. Design taken from tyche (Apache-2.0, attributed in NOTICE); midas influenced ideas only, no code. | Seven-agent reconnaissance read all three codebases before the call. Adopting a shell would have meant paying the full backend cost anyway, then maintaining a second runtime to display it, then writing the trading surface regardless — while giving up the ability to test the terminal's contract under the repo's existing strict gates. | ADR-0018 |
| D-20 | **Reconciliation readiness is re-established on a bounded periodic cadence, and expires on age.** A writer-only lifespan task re-runs `reconcile_submission_readiness()` every **120 s** while positioned in RTH, **240 s** flat in RTH, and **1800 s** while the market is closed; readiness older than the **300 s** maximum evidence age reads as `PENDING` regardless of the loop's health, and readiness never crosses a regular-session open. Closes plan §6 finding 1. | Today the first opening order consumes readiness (`submission_guard`) and nothing re-arms it, so every later opening order is blocked until restart. Re-arming it is **widening in effect** even though it restores intended function, so it is owner-gated and recorded rather than slipped in. The cadence is set by what can change the book without an order this system placed — a fill on a resting limit, and overnight assignment — and bounded on the other side by the shared 6-per-window pacing budget, where headroom is a safety property because rate limit spent watching is rate limit unavailable to cancel. Two consecutive failed cycles fail closed by arithmetic rather than by error handling. | ADR-0020 |
| D-21(b) | **[Annotated 2026-08-22: duplicate number, never renumbered — see the note on D-21(a) above.** External citations of "ADR-0025/D-21" mean this row.**]** **The owner builds, Chronos trades — live experimentation authorized at owner-capped size.** Owner directive (2026-08-09): "I'm willing to risk my own capital to see if this AI algo trading bot can trade like me or better. I want to outsource my day-trading tasks to AI. This is my hobby: I build and improve Chronos, while Chronos trades and implements my investment strategies." The promotion ladder is re-scoped to gate **claims** ("validated", "proven", "better than the owner"), which keep the frozen evidence bar unchanged; owner-authorized live trading at bounded size instead waits on the ADR-0025 go gate (funded capital + typed loss limits, read-only gateway campaign, ≥20 paper lifecycles / ≥10 sessions, TradingView parity export, market-data subscription, owner-authored mandate + kill drill — frozen before observation). Finding 4 resolves toward standing mandate authority (arming retirement is follow-up, owner-reviewed work; code wins until it lands). Rungs above SHADOW stay owner-declared per the ADR-0024 interim rule — D-21 records the intent, the owner types the mandate. Every execution-correctness mechanism (single transmit site, ten-gate stack, ADR-0009 conjunction, kill precedence, deny-by-default mandates, model isolation, registry/replay discipline) is explicitly unweakened. Live P&L at small N is anecdote, recorded as such — capital risk buys experience, not evidence. | The owner's stated relationship to the project: a hobby whose purpose is delegated trading. Forbidding owner-capped live experimentation until a 24-36-month ladder completes serves no one the safety machinery protects; laundering that experimentation into evidence would corrupt the discipline that makes the experiment informative. This row separates the two cleanly, the same move D-16/D-17 made for autonomy authority. | ADR-0025 |
| D-17 | **Maximal autonomy, owner-directed.** A persistent mandate file (`AUTONOMY_MANDATE_FILE`) auto-activates on every boot — a running backend plus a valid, account-matching mandate is enough to trade, with no per-boot ritual (supersedes ADR-0016 §4 "an env var alone may not activate live"). The live-mandate ceiling moves 30d→365d and `restart_behavior` defaults to `RESUME_UNTIL_EXPIRY`. A `model_discretion` grant inverts deny-by-default **for capital ceilings only**: an unset ceiling means "affordability is the bound," while any ceiling the owner *did* set still binds. `OrderForm.MARKET` exists but compiles to a **protected** collared limit (quote±1%), never an unbounded venue `MKT`; the compiler prefers the most-aggressive *granted* form. **Not superseded:** floors/reserve, single transmit site, writer lease, kill switch, reconciliation, stale-data refusal, the full propose→preview→confirm→submit handoff, the degraded-state rule, and deny-by-default for everything the owner did not grant discretion over. Revocation survives restart; an invalid/wrong-account file boots inert with a CRITICAL alert. Closes the R-36 wiring residual. | Owner directive (2026-07-25): Chronos "as close to autonomous as possible," modeled on the reference Quant-Guild bot. "Maximal" was scoped as removing friction and owner-optional ceilings, not execution-correctness mechanisms; market orders implemented as protected rather than unbounded, with the literal-unbounded interpretation flagged as a separate un-taken decision. | ADR-0017 |
| D-22(b) | **[Annotated 2026-08-22: duplicate number, never renumbered — see the note on D-22(a) above.** External citations of "ADR-0026 / D-22" mean this row.**]** **A TradingView alert may become a Chronos proposal, through a separate unprivileged bridge process that grants nothing.** `chronos.bridge` listens for a TradingView webhook, authenticates it against an owner-configured shared secret, translates it into candidate proposal JSON, and POSTs that to the *existing* loopback `/autonomy/proposals` with the *existing* local API token. It is a client of the ingress, not a new door into it: the route, the parser, and every downstream gate are unchanged, so a TradingView-sourced proposal walks the same fifteen admission checks, the same sizing against mandate ceilings and floors, the same capability-matrix compiler, and the same propose→preview→confirm→submit handoff. The load-bearing choice is that the bridge **does not import `chronos.autonomy`** — it emits a dict and the ingress decides whether that dict is a proposal, so there stays exactly one place where "what a valid decision is" is decided. The resulting duplication is guarded structurally: an isolation test pins every restated enum *equal* to the real one, and an exercised test pushes the bridge's own output through the real `ingress.parse_proposal` per decision kind. Ships inert — `CHRONOS_TV_BRIDGE_FORWARD` defaults false, both allowlists are required, the secret is mandatory, and a non-loopback ingress URL refuses to boot. Equities only; the four ADR-0021 inert fields are deliberately never populated. **Not superseded:** ADR-0016 §8 and ADR-0017 §5 in full; the mandate remains the only grant of trade-time authority and the bridge cannot read, name, write, or activate one. An alert is not evidence and creates no promotion rung. | Owner directive (2026-08-12) following D-21: the owner's strategies already take the form of TradingView alerts, and a webhook is the only export TradingView offers. Adding a network channel is owner-gated + new-ADR work, so the mechanism is built inert and the three live decisions (publishing the listener, enabling forwarding, and whether alerts are an appropriate source for this account) stay with the owner. Disclosed residual, sharpened rather than closed by this row: provenance still cannot distinguish a TradingView-sourced proposal from a model-worker one, because both are stamped with the static `INGRESS_IDENTITY` — the bridge compensates with a digest-bearing evidence citation, and plan §6 finding 6 / ADR-0023 stay open. | ADR-0026 |
| D-23 | **The model worker exists: an in-repo, out-of-process AI brain that proposes and only proposes.** `worker/` (top-level, deliberately NOT part of the `chronos` package or wheel; run as `python -m worker`) implements the external worker ADR-0016 §3 designed the autonomy stack around: each cycle it reads the backend's token-protected read endpoints into a digested canonical-JSON evidence snapshot, sends it to Claude with the owner's editable trading policy (`worker/policy.md`), forces one `propose_decision` tool call (`strict: true`, `additionalProperties: false` — provenance, decision_id, accounts, prices, routing and transmit are unrepresentable in the schema, not filtered from it), validates the result as hostile input against a restated-and-pinned vocabulary, stamps an evidence citation whose digest is the SHA-256 of the exact bytes the model saw, and POSTs the candidate to the existing loopback ingress. Three structural choices carry it: the worker imports NOTHING from `chronos` (AST + subprocess probe; converse also pinned — nothing in `src/chronos` imports it); no LLM SDK enters the repo's dependency tree (raw httpx to the Messages API; the old manual grep is now a failing test); and the model's only output channel is the forced tool, so ADR-0016 §5's "prose is never parsed into orders" is the transport, not a discipline. Ships inert: `CHRONOS_WORKER_FORWARD` defaults false, watchlist and kind allowlists required, loopback-only backend URL, evidence gathered or the cycle refuses to think, model refusal/truncation/prose → no proposal. **Not superseded:** every gate; the mandate remains the only authority and a worker with no mandate produces judged refusals, not trades. A worker decision is not evidence and touches no registry or rung. | Owner directive (2026-08-12), completing D-16/D-21: the fortress was built around an empty throne — every control on the proposal path existed and no model had ever proposed. Building the worker in-repo (but out-of-package, out-of-process) keeps it testable under the repo's gates while preserving the ADR-0016 §3 process boundary and the no-SDK/no-key invariants of the broker-holding process, verbatim. Residuals sharpened, not closed: static `INGRESS_IDENTITY` now covers three indistinguishable sources (worker, TradingView bridge, any token-holder) — plan §6 finding 6 / ADR-0023 is overdue; the policy file is unpinned strategy authority pending the same protocol; R-30 prompt injection stays bounded-not-solved with the snapshot as the new surface. | ADR-0027 |
| D-24 | **Proposal authorship is a registered, revocable credential — never a shared token, never the payload's claim.** ADR-0023 Option A: `AUTONOMY_PROPOSERS_FILE` names an owner-authored registry (`chronos.supervisor.proposers`) binding each proposer's **credential hash** (SHA-256 only — the file cannot yield a credential) to the exact identity facts the queue writer stamps: provider, model, model/prompt/tool/decision-schema/policy versions, a hard expiry, and an enable flag. Unset, nothing changes: proposals authenticate with the local API token and carry the static ingress identity, exactly the pre-registry posture. Set, the posture flips whole: `POST /autonomy/proposals` requires a registered `X-Chronos-Proposer-Token` (the general token is refused there, with a message that says why), the route records the *verified* proposer_id on the durable queue row, and the drain re-resolves it into a `HarnessIdentity` **at the moment authority is exercised** — a registration that expired between enqueue and drain refuses at STAMP (`PROPOSER_UNRESOLVED`), and a configured-but-broken registry refuses every proposal (route 503 + STAMP) with a CRITICAL alert rather than falling back to anonymous proposing. The registry itself is a boot-time snapshot, exactly like the mandate file: disabling or deleting a registration takes effect at the next backend restart, expiry is the transition the snapshot carries live, and ~~the mid-session stand-downs remain the kill switch, mandate revocation, and a restart.~~ **[Corrected 2026-08-14 by D-26/A3: a single leaked credential is now stood down live by `chronos.cli proposer revoke`, a durable hash-chained act both planes consult per check — no restart. The rest of the sentence stands: enabling and re-registering are still restart-scoped, deliberately.]** The credential is proposal-only in both directions, exercised route-by-route over the app's own table; provenance and the hash-chained proposal journal now carry `proposer_id`; and the placeholder evidence digest became honestly absent (`None`) instead of sixty-four zeros, with admission's check 9 refusing a digest disagreement in *every* direction (claimed-when-absent included). `chronos.cli proposer mint|check` are stdout-only — a tool that mints text is not a tool that grants. Building the exercised proofs also surfaced and fixed a live inert-control defect: the proposal route's `_fingerprint_of` read an attribute `AppRuntime` never had, so **every** real proposal POST had answered 503 `BACKEND_UNSCOPED` (and `GET /autonomy/alerts` answered `[]`) since M7 — fail-closed in direction, dead-ingress in effect. **Not superseded:** the mandate remains the only grant of trade-time authority; registration is identity, not authority — a registered proposer can do exactly one thing an anonymous caller cannot, which is be *attributed*. | Owner directive (2026-08-12): "lets build the worker identity protocol next" — Option A built directly, delivering ADR-0023's recommended Option B (the credential split) as a subset. Closes plan §6 finding 6: provenance now authenticates an author, not the ingress, and the mandate's version pins finally bind a grant to a registered authorship — a mandate pinned to the worker's registration refuses the bridge's proposals at admission check 8. Disclosed bounds: authorship pins by version *tuple*, not by name (two registrations sharing identical version fields are indistinguishable to admission; `mandate check` lists every match); evidence binding stays uniform (registrations carry no evidence fields; the job/evidence protocol is future ADR work); identity resolution trusts the owner-secured registry file the way the mandate loader trusts the mandate file. | ADR-0023 |
| D-25 | **Evidence binding is a per-job record the backend issues, and admission check 9 finally has two sides.** ADR-0028 Option C. The finding that justified building it: check 9 compared `provenance.evidence_bundle_id`/`_digest` against `SupervisorState.expected_*` and **both sides were two reads of the single `INGRESS_IDENTITY` constant** — one copy stamped by the queue writer, the other copied into `CycleFacts` by the backend gatherer — so the check could not refuse, in any posture, for any proposer. Written correctly, wired to a comparison that had never had two independent origins: the R-24..R-27 shape one level up. `AUTONOMY_EVIDENCE_BUNDLES` unset changes nothing (proven byte-identical to `b5d61dd` against a recorded journal row, not by inspection). Set, the posture flips whole: `POST /autonomy/evidence` issues a durable, hash-chained bundle record (`autonomy_evidence_bundles`, migration 0008, schema v9) to the *presenting credential* — either `backend_served`, where the backend composes a canonical document and digests **the exact bytes it serves**, or `alert_attested`, where a proposer asserts a digest of bytes Chronos never saw. The drain resolves the cited bundle against that record **at its own clock**, so unissued, foreign-proposer, and expired-between-enqueue-and-drain all refuse at STAMP before the proposal is judged, and provenance is stamped from the RECORD. Check 9 then gains the independent side it never had: the payload's own `EvidenceCitation` — authored by the proposer, never written by the backend — must name the expected bundle, carry a citation kind that record's kind admits, and match its digest. Three additive refusal codes (`EVIDENCE_BUNDLE_EXPIRED`, `EVIDENCE_BUNDLE_UNCITED`, `EVIDENCE_BUNDLE_KIND_MISMATCH`); nothing existing weakens. Evidence binding without a proposer registry refuses every proposal with a CRITICAL alert and never falls back — a bundle is issued *to* a credential. Building the exercised proofs also surfaced and fixed a second live dead-route defect: `chronos.api.bars.provider_for` cached by assigning a new attribute to the `slots=True` `BackendState`, so **`GET /terminal/bars` answered 500 for every symbol, on every backend, since the route existed** — no test had ever called it. **Not superseded:** the mandate remains the only grant of trade-time authority; a bundle authorizes nothing and every gate downstream is unchanged; registration stays identity, not authority. | Owner directive (2026-08-14): "merged 69, go with option C, have opus build it" — Option C chosen over A, B and D because it is the only one under which check 9 stops being a tautology (A and B each move the constant to a per-job value without ever making the comparison meaningful). Closes the evidence half of plan §6 finding 6; the identity half closed 2026-08-12 as D-24. Disclosed bounds, all carried in R-50: **equality catches accident, not malice** — a proposer that fetches a bundle, reasons on other text and cites the issued digest is undetectable, because the backend cannot observe a prompt in another process; **attested is not witnessed**, so an attested bundle may back a proposal and never a promotion rung (ADR-0024's to adopt or reject); a bundle binds which facts were *served*, never that they were true — every fact still comes from a fixture-verified adapter path and nothing here leaves MITIGATED; requiring digest equality couples the backend's serialization to the proposer's rendering, version-pinned via `bundle_version` and fail-closed when they drift; the 300 s TTL and its 3600 s ceiling are disclosed judgments, not derived numbers; and issuance widens a proposal-only credential to a second route, which R-48's per-route enumeration now carries as its one named, tested exception rather than absorbing. | ADR-0028 |
| D-26 | **A leaked proposer credential is revoked by a durable act both planes honor live — keyed on the credential, not on the proposer's name.** `chronos.cli proposer revoke` writes one row in `autonomy_proposer_revocations` (migration 0009, schema v10) and one hash-chained record in the un-scoped `autonomy.proposers` stream; `require_proposer` consults it on every route authentication (401) and the drain-time identity resolver on every STAMP (`PROPOSER_REVOKED`, a new code so the journal can tell a credential the owner killed from a registration that merely aged out). Nothing is cached. Revocation is permanent for that credential and survives restart; there is no un-revoke. | R-48 residual (c) disclosed that the registry is a boot-time snapshot on both planes, so standing down a leaked credential meant editing the owner's file and **restarting the process that holds the broker connection** — during the incident, which is when an operator least wants to bounce it. The keying choice is the operational one: keyed by `proposer_id` a revocation would burn the *name* forever and force the owner to invent `claude-worker-2` mid-incident; keyed by `secret_sha256` it kills exactly the secret that escaped, so re-minting a fresh credential for the same proposer works at the next restart while the leaked one stays dead permanently. The row records the id alongside for legibility. The read is durable and per-check rather than another snapshot — that is the entire point — and fail-closed: an unreadable ledger refuses (503 at the route) rather than admitting. The registry file itself deliberately stays a boot-time grant for everything else; revocation is the one act whose correct latency is *now*. Deliberately not owner-gated to build: it only ever removes authority. | — |
| D-28 | **The model worker may call xAI Grok as a second provider, through the same process, the same policy file, and the same loopback ingress.** `CHRONOS_WORKER_PROVIDER=xai` plus `XAI_API_KEY` (console key only — never `~/.grok/auth.json`) selects a raw-httpx Chat Completions transport (`worker/model_xai.py`) that forces `propose_decision` and deny-by-default extracts the function call. Anthropic remains the default; both paths stay inert (`FORWARD` false). Register Grok as its own proposer (`grok-worker` / `xai` / `grok-4.6`); sharing the Claude credential would lie about authorship. Residual: xAI does not advertise Anthropic `strict: true`; local vocabulary validation and the gateway remain the real gates. | Second provider, not a second authority: no new grant, no new network channel on the broker process, no default-forward change. Implementation of D-23 / ADR-0027. | — |
| D-32 | **The hourly bars lane exists, separately.** `bars_1h/<SYMBOL>.csv` with real close timestamps and stored `session_date`; interval-conditional `Bar.sequence_id` (DAY_1 byte-identical — it participates in execution intent identity); IBKR requests pin `formatDate=2` + `useRTH=1`; chunked, paced backfill with empty-chunks-are-skips; hourly certification judges BARS against `expected_close_timestamps_utc`, with the frozen 99.5% floor binding the bar ratio and corporate actions reconciled in the derived daily-close frame; hourly adjusted views refuse (C_ref is the official daily closing print); evidence schemas bumped to v2 while zero production digests existed. Executable trading scope is NOT reopened — D-12/ADR-0008's account-economics rationale stands. | Phase 3 commits to a daily AND hourly certified export, and the daily binding ran deeper than the hardcoded request: the sweep before this build found two silent-wrong paths (the `allow_correction` last-bar-wins collapse; the date-keyed dedup identity) and a coverage definition in which a session holding one of seven bars counts as covered. Full rationale, boundaries, and the owner-verify list: ADR-0029. | [ADR-0029](docs/adr/ADR-0029-hourly-bars-lane.md) |
| D-31 | **"Certified data" becomes a verdict with a digest, not a phrase.** `research.certification` judges an export against the four Phase 3 gates and returns findings, never warnings; `research.dataset_release` freezes a passing export into content-addressed partitions and emits the exact `chronos-certified-data-catalog-v1` manifest `research.certified_data` has authenticated since C3. `scripts/certify_dataset.py certify|freeze` is the owner-facing command. The corporate-action gate is split honestly: code reconciles the action stream against the prices, and the *independent sample* is an owner attestation without which the export is refused. Clean holdout spans become catalog `holdout`; seen and burned become `ordinary`, and a burned span must record why it was consumed. | Phase 3 froze four data-quality gates before collection and none of them had an executable form, so D2 was an owner action with no definition of done — and the catalog reader had gone since C3 with **no producer at all**: the only manifests in existence were hand-built inside tests. Three properties are load-bearing. Coverage is measured against `session_calendar`'s independent expectation, because a vendor that drops forty sessions ships a file perfectly consistent with itself. Both error directions block, so a wrong calendar entry fails loudly instead of certifying a hole. And the report carries no timestamp or host, so the digest is a pure function of the bytes it judged and re-certifying identical data reproduces it. Verified end-to-end on the in-repo QQQ history: 6087/6087 sessions matched across 24 years including the September 2001 and Sandy closures, and the unadjusted March 2000 2-for-1 split surfaced as an `UNCLASSIFIED_MATERIAL_MOVE` until the action stream explained it — the gate finding a real defect in real data, which is the only evidence a gate is worth having. | — |
| D-30 | **The research plane gets an expected-session calendar; the trading plane still does not.** `chronos.research.session_calendar` derives NYSE/NASDAQ sessions from pinned rules (recurring holidays with Rule 7.2 observance, Good Friday by computus, Juneteenth from 2022) plus a table of ad-hoc closures no rule can produce (9/11, Reagan, Ford, Sandy, Bush, Carter), and refuses any date outside the range that table is complete for. Half-day knowledge is pinned over a narrower window than session knowledge and `expected_bar_count` refuses an hourly question outside it rather than counting a 13:00 close as 16:00. `tests/safety/test_session_calendar_isolation.py` bars every authority package from importing it. | Phase 3's first data-quality gate is "at least 99.5% **expected**-session coverage", and expected is not derivable from the delivered bars — a vendor that silently drops 40 sessions ships a file perfectly consistent with itself. Certification needs an independent expectation, so D2 had no runnable coverage gate without one. **The R-26 boundary is why this is research-only and structurally fenced:** R-26 settled that market-open evidence comes from the venue's own `CLOSED` token because a derived calendar that wrongly says "open" opens a gate that should have held. Nothing here reverses that — the one hazard a calendar introduces is a later wiring change, and the isolation test is the answer to it. The error direction is also deliberate: this classifies discrepancies and never suppresses them, so a session with no bar blocks certification and a bar on a non-session day blocks it too. A wrong entry fails loudly at certification rather than silently certifying a hole — which is what makes a pinned table acceptable as evidence infrastructure and unacceptable as a trading gate. | — |
| D-29 | **Deep-trading research candidates selected: C1 deep momentum network (Lim/Zohren 2019) and C2 CNN/LSTM direction classifier (Sezer/Ozbayoglu family), each inseparable from its deterministic twin (classical TSMOM with identical vol scaling; logistic regression on identical features).** Everything stacks behind the Phase 3 certified IBKR export: the preregistration revision (H-DT-001/H-DT-002, FIVE_TOOL_RESEARCH_HYPOTHESES pattern, blocked before data access) is the next research artifact after the release digest exists, and the `research/deep/` build follows under the five_tool isolation invariants with content-addressed weights. DeepLOB/HFT, RL, crypto, and sentiment remain out of scope per the recorded feasibility verdicts; RL additionally requires its own ADR if ever revisited. | Shared data contract and trial frame; architecturally disjoint enough that the results say different things; C2 doubles as harness validation for C1. Source: docs/DEEP_TRADING_FEASIBILITY.md (PR #79); owner pick 2026-08-21. | — |
| D-27 | **A registration's `prompt_version` is derived from the policy file's bytes, not typed by hand — and that binds the file, never the run.** `proposer mint --policy-file` sets `prompt_version = sha256(policy bytes)[:16]`; `proposer fingerprint --policy-file` prints the same derivation so a mandate can be re-pinned without rotating a credential; `proposer check --policy-file` reports per registration whether the policy on this machine still matches what was registered. Pinning is opt-in: mint without the flag and the prior typed-label behavior is unchanged. Passing both `--policy-file` and `--prompt-version` is refused rather than resolved by precedence. | R-47 residual (b): `worker/policy.md` is the strategy persona, and nothing bound it to the label a mandate pins against — so editing the policy left the mandate's `VersionPins` still agreeing, and two runs under materially different instructions were indistinguishable in the journal. Deriving the label from the bytes means an edited policy stops matching and admission's existing check 8 refuses `VERSION_PIN_MISMATCH`; no new refusal was added, an existing one stopped being fooled. **The bound, stated because this is the decision most likely to be over-read:** it does not prove which policy the worker *ran*. The worker reads `CHRONOS_WORKER_POLICY_FILE` at startup and nothing in Chronos observes that read, so the digest binds the file as it was at mint time. `check --policy-file` narrows this to drift detection on one machine — the strongest claim available without the worker attesting its own digest, which is a worker change and deliberately not this item. | — |
| D-33 | **The steering package: `main` becomes the default branch, integrity rules bind it, and one operating protocol binds every seat.** On owner direction (2026-08-22) the GitHub default branch flipped from `feat/wheel-dashboard-mvp` (now deleted) to `main`. The `main-integrity` ruleset makes `main` PR-only with the `quality` check required green, no force-push, no deletion, and no bypass actors — every agent pushes as the same account, so no seat is special. `delete_branch_on_merge` is on. Superseded history worth keeping goes to `refs/preserve/<seat>/<name>` refs (no CI, no branch clutter), never a PR. And `docs/AGENT_PROTOCOL.md` is adopted as the operational protocol for all seats: preflight, branch/PR rules, gates, review semantics (HOLD closed only by the holder in writing), post-merge ancestry verification, and scan-before-claim ID allocation. | The repo's process lessons lived only in commit messages and one morning's incidents — the phantom merge (PR #77's un-strand), the empty review that wasn't a pass, the HOLD fixed but never withdrawn, and PR #82's stale-base archive push were all the same failure class: an unprotected default, divergence computed against the wrong base, and rules that existed nowhere a seat would read them. Settings enforce what settings can (ruleset, auto-delete); the protocol document carries the rest, with volatile facts as commands so it cannot go stale the way sixteen skills did. Infrastructure + process; no architectural decision. | — |
| D-34 | **Accepted (2026-08-01): autonomous option selection is a deterministic, read-only, receipt-producing gate, initially limited to `OPEN` equity-option cash-secured puts and covered calls.** The model cannot name a right or broker contract identity; the economic request carries only a strategy-derived right. Complete, bounded, timestamped qualification, chain-completion, quote/liquidity, session, authoritative-deliverable, and market-rule evidence must pass before a versioned total-order resolver may select and derive a receipt-bound limit; otherwise the result is typed `NO_TRADE`. The existing compiler independently reproduces the exact contract/price. Canonical receipts are committed to an account-scoped hash chain and semantically replayed before handoff. The feature defaults off, and each autonomous CANARY/LIVE mode requires a separate owner-created artifact matching the canonical mandate, policy, and every material resolver version/digest at initial, post-acquisition, and pre-handoff checks; runtime code cannot create or promote it. No artifact is shipped. Real IBKR remains `NO_TRADE` because TWS exposes no authoritative deliverable schedule. | Determinism is meaningful only when the complete evidence and exact policy that produced a contract are replayable. Keeping identity out of model input, mutations out of the selector, and promotion outside runtime prevents missing or changed evidence from silently becoming execution authority. | ADR-0030 |
| D-35 | **Accepted (2026-08-25): QQQ v1 is a staged, daily-close research constitution with no present trading authority.** QQQ is the only execution target; SPY, IWM, DIA, GLD, and TLT are validation-only. The first campaign is simple long/short trend, followed only as separately preregistered campaigns by long/short mean reversion, a regime-combined strategy, and model-selected signals; evidence never transfers between them. The current live allocation and live risk are USD 0. USD 3,000 is a research reference and conditional future funding target after an untouched-holdout pass, at least 90 days of shadow evidence, supervised-paper evidence, and fresh owner approval. The frozen research gates are volatility-matched QQQ/cash as primary benchmark (raw QQQ buy-and-hold also reported), at least 4 percentage points annualized post-cost alpha with a positive 95% lower bound, 10% maximum drawdown, 100% gross exposure, 1x leverage, a USD 60 (2%) observed daily/session halt, and USD 45 (1.5%) daily 95% CVaR. Decisions use confirmed daily closes for next-session execution. Incremental recurring data/software budget is USD 0; uncertifiable evidence blocks. The short side remains blocked by compiler refusal and missing account/borrow/cost evidence. The artifact selects no strategy, opens no holdout, runs no trial, and grants no submission, promotion, funding, or short-selling authority. | Freeze the economic and safety claim before reading new evidence, while preserving fail-closed separation between a research target, future funding, and actual authority. | ADR-0031 |
| D-36 | **Accepted (2026-08-25): the QQQ v1 trend preregistration will use confirmed daily closing price relative to a trailing 200-session simple moving average as its direction indicator.** This selects one input form only. The exact price series and window convention, equality behavior, state transitions, entry/exit rules, persistence or buffer, sizing, short-side asymmetry, and parameter-neighbor tests remain unresolved and must be frozen together before any campaign data read. The immutable D-35 constitution remains unchanged, `selected_strategy` remains `null`, and this decision runs no trial and grants no trading authority. | Of the four considered first rules (12-month return sign, price/SMA-200, SMA-50/200 crossover, 252-day breakout), price/SMA-200 has the closest structural fit with the Five-Tool Confluence's existing moving-average direction filters while remaining simple enough to isolate. This is a design-synergy judgment, not performance evidence. | ADR-0031 |
| D-37 | **Accepted (2026-08-25): the QQQ v1 primary trend cell uses an immediate two-state direction transition with no neutral buffer or confirmation delay.** Once initialized, the signal changes from long to short, or short to long, on the first confirmed daily close strictly across the trailing SMA-200; any action remains no earlier than the next session. A 1% neutral band and a five-consecutive-close confirmation rule are preregistered robustness variants, never post-result rescue choices. This defines signal state only: exposure sizing, protective exits, price/window/equality initialization details, short-side asymmetry, and all authority remain unresolved. | It is the lowest-degree-of-freedom attribution cell, while the Five-Tool Confluence already contains separate hysteresis and confirmation logic. Keeping those overlays out of the primary cell avoids double-filtering and lets the registered neighbors measure whether delay or a buffer adds value after costs. | ADR-0031 |
| D-38 | **Accepted (2026-08-25): QQQ v1 sizes each direction by a deterministic volatility scaler whose binding objective is estimated daily 95% loss-CVaR at or below 1.5% of the applicable capital base (USD 45 at the USD 3,000 research reference), with gross exposure capped at 100% and leverage capped at 1x.** The cap is a ceiling, not a target or permission to trade. Missing, stale, non-finite, or otherwise uncertifiable CVaR evidence cannot produce exposure. The exact estimator, lookback, capital-base convention, rebalance rule, minimum trade threshold, and short-side treatment remain open and must be frozen before data access. Fixed 100%, fixed 50%, and ATR-stop sizing are not the primary method. | This turns D-35's frozen tail-risk limit into the sizing objective instead of hoping a fixed notional happens to satisfy it. It also aligns candidate exposure with the volatility-matched benchmark while preserving deterministic caps and fail-closed evidence handling. No performance benefit is presumed. | ADR-0031 |
| D-39 | **Accepted (2026-08-25): the QQQ v1 primary risk estimator is empirical 95% historical loss-CVaR over the latest 252 completed daily unit-exposure return observations available at the confirmed-close decision time.** It is the arithmetic mean of the 13 greatest observed losses (`ceil(5% * 252)`), with no distribution fit or quantile interpolation. Fewer than 252 finite observations produces no risk estimate and therefore no exposure. The exact return/price convention, whether the current completed session enters the window, long-versus-short tail construction, rebalance rule, and capital base remain open. The 126-session, 504-session, and EWMA estimators are not primary. | One completed trading year is transparent, reproducible, and avoids a parametric tail assumption while remaining more responsive than a two-year window. The sparse 13-observation tail and backward-looking nature remain limitations; choosing it is not evidence that it predicts future losses. | ADR-0031 |
| D-40 | **Accepted (2026-08-25): QQQ v1 constructs long and short CVaR tails independently.** Long sizing uses the 13 greatest losses from the 252 completed long unit-exposure return observations; short sizing uses the 13 greatest losses from the corresponding short unit-exposure observations, so sharp QQQ rallies—not QQQ selloffs—populate the short market-loss tail. Applicable certified costs belong in the direction's return stream. Long CVaR is never mirrored onto shorts. Without a content-addressed borrow-cost schedule and the remaining short evidence required by D-35, short CVaR is uncertifiable and short exposure remains zero. Return/price semantics, window inclusion, capital base, and rebalancing remain open. | Long downside and short-squeeze tails are economically asymmetric. Separate construction measures the loss distribution of the position actually proposed, while a shared or mirrored estimate could understate short risk. This design choice is not evidence that either direction is profitable. | ADR-0031 |
| D-41 | **Accepted (2026-08-25): the QQQ v1 CVaR capital base at each confirmed-close decision is the lower of current marked strategy NAV and the USD 3,000 research reference.** The dollar tail-risk ceiling is therefore `min(1.5% * applicable_base, USD 45)`: it shrinks after losses and cannot ratchet above USD 45 after gains. Non-positive, missing, stale, or non-finite NAV produces no exposure. The USD 3,000 ceiling can change only through a fresh owner decision; current live allocation remains USD 0. Exact NAV composition and rebalance mechanics remain open. | This makes loss-driven de-risking automatic without allowing performance to silently expand the owner-frozen risk budget. It preserves the distinction between research normalization, actual funded capital, and trading authority. | ADR-0031 |
| D-42 | **Accepted (2026-08-25): QQQ v1 uses asymmetric rebalance timing.** Risk evidence and target exposure are recomputed at every confirmed daily close. Any required gross-exposure reduction—including closing the old direction after a signal flip—is eligible at the next session; increases, including establishing the new direction, are eligible only on the frozen weekly increase schedule. A halt, stale input, or failed gate can reduce or suppress exposure but never accelerate an increase. The exact weekly anchor, whole-share rounding, minimum economic trade threshold, and order semantics remain open. | Tail-risk breaches should de-risk promptly, while daily increases would turn estimator noise and whole-share granularity into avoidable turnover and commission drag. Asymmetric timing makes safety faster than risk expansion. | ADR-0031 |
| D-43 | **Accepted (2026-08-25): QQQ v1 converts permitted target exposure to whole shares by rounding magnitude down toward zero.** Quantity is `floor(permitted_target_notional / sizing_reference_price)` with the signal direction applied afterward; a target below one share produces zero exposure. Nearest-share, round-up, and fractional-share sizing are excluded from the primary cell. The exact sizing-reference price, next-session gap handling, pre-handoff revalidation, and minimum economic rebalance threshold remain open. | Rounding down cannot intentionally push notional above the CVaR-derived target, while nearest or upward rounding can. It also preserves the repository's disclosed whole-share economics without introducing fractional-share execution assumptions. | ADR-0031 |
| D-44 | **Accepted (2026-08-25): QQQ v1 computes its SMA-200 signal and CVaR return streams from a point-in-time total-return-adjusted QQQ series derived only from unadjusted bars and corporate-action evidence known by that confirmed close; order quantities, prices, fills, and broker reconciliation remain on raw tradable prices.** Long returns include distributions; short returns include the corresponding distribution liability when certified. Future corporate actions may never rewrite an earlier decision's information set. Missing or uncertifiable action evidence blocks the campaign rather than falling back to raw or silently back-adjusted data. Current-session window inclusion remains open. | Total-return data measures the economic position and avoids treating an ex-dividend price drop as trend deterioration, while raw prices remain the only truthful execution identity. Point-in-time derivation prevents future action knowledge from leaking into historical decisions. | ADR-0031 |
| D-45 | **Accepted (2026-08-25): QQQ v1 includes the newly confirmed session in both decision windows.** At close `t`, SMA-200 uses the 200 completed point-in-time total-return closes ending at `t`, and CVaR-252 uses the 252 completed direction-specific daily return observations ending at `t`. No intrabar or future value enters either window; any reduction or weekly-permitted increase remains first eligible after the decision, never at close `t`. Fewer than the full required observations produces no signal/risk estimate and no exposure. Exact equality/initial-state behavior remains open. | The current completed session is available information, so including it is fresher without lookahead. Lagging it would add a one-session delay with no information-integrity benefit. | ADR-0031 |
| D-46 | **Accepted (2026-08-25): QQQ research separates the simple SMA-200 attribution control from the integrated Five-Tool Confluence candidate, and selects exit option 4 for the Confluence cell.** The control keeps immediate SMA-200 transitions, a signal-flip exit, and CVaR-primary sizing. The integrated cell preserves the pinned source's native EMA-100, two-bar hysteretic regime, score/trigger entries, and exact layered protection: structural stop or 2-ATR fallback, 1R/2R targets, breakeven after T1, 22-bar/3-ATR Chandelier runner after 1R, opposite-regime exit, and the source's side-specific AVWAP invalidation. Integrated sizing starts with the source-native 1% stop-risk quantity on D-41's capped base, then takes the minimum allowed by direction-specific CVaR-252, the 100% gross/1x and affordability ceilings, and whole-share round-down. The owner-selected 2%/USD 60 daily/session limit remains a circuit breaker, not per-trade risk. D-42's weekly increases are superseded: reductions remain daily/next-session, while increases occur only on a new confirmed entry event while flat; up to three source-native same-event management legs do not authorize a later top-up. D-39–D-41 and D-43–D-45 remain outer controls, while the integrated EMA/ATR/AVWAP/support price-domain mapping under D-44 remains blocking. The exact Pine default asymmetry remains explicit—short v2/SHORT+ on, long v2 off—and changing it is a separate variant. No trial, selection, short authority, funding, submission, or live authority results. | A single blended rule would destroy attribution and still fail to reproduce the owner's Confluence. Two identities preserve the scientific control and the actual strategy; native stop-distance sizing and event-driven entries retain Confluence semantics, while CVaR and the stricter owner limits remain independent vetoes. | ADR-0032 |
| D-47 | **Accepted design (2026-08-25; exact artifact remains owner-gated at merge): freeze the QQQ SMA control as a content-addressed, refuse-closed five-cell preregistration.** Warmup stays flat; first strict inequality initializes direction; later equality holds state. Signal-time sizing uses the confirmed raw close, next-session revalidation may only reduce quantity against Chronos's existing 1% protected-market collar, each entry event gets one DAY attempt with no chase/top-up, and projected round-trip cost must not exceed 10% of the applicable CVaR budget. The grid is SMA-200 primary plus one-axis SMA-150, SMA-250, 1% neutral-band, and five-close variants. | These choices preserve a simple attribution control while testing anti-whipsaw behavior prospectively, match the Confluence's broad shape without importing its native EMA/regime/score stack, and align entries with Chronos's fail-closed gap, authority, and cost posture. It grants no data, trial, short, order, promotion, paper, or live capability. | ADR-0033 |
| D-48 | **Accepted design (2026-08-25; exact artifact remains owner-gated at merge): freeze a content-addressed QQQ overlay over the unmodified Five-Tool v3.6 source.** All technical geometry uses causal point-in-time total-return OHLC rebased so the confirmed close equals the raw tradable close; split-adjusted current-share volume weights AVWAP, while quotes/orders/fills remain raw. An intervening corporate action invalidates the entry. Source defaults stay exact, including master shorts off, short v2/SHORT+ on behind it, and long v2/LONG+ off. Native 1% stop risk and layered exits remain inside CVaR/gross/owner caps, one-shot protected DAY entry, all-in cost floor, and no top-up. | This prevents corporate actions from fabricating technical signals without sending synthetic price levels to the broker, preserves source fidelity and intentional side asymmetry, and keeps every missing identity or execution capability fail-closed. It grants no data, trial, short, order, promotion, paper, or live capability. | ADR-0034 |
| D-49 | **Accepted design (2026-08-25; owner-gated at merge): add a default-off, proposal-only QQQ PAPER position-management state machine without creating a second authority surface.** Actual opening fills bind the exact D-48 candidate and management-policy digests, whole-share leg geometry, stop/CVaR evidence, account, position, and opening-order identities. Hash-chained events replay observations and typed resolution facts; risk exits, 1R/2R targets, actual-fill breakeven, and the 22/3 Chandelier runner can emit only `ProposedDecision` risk reductions with `execution_authority="none"`. | A durable lifecycle is required before PAPER can be credible, but recording broker truth must not itself grant permission to trade. The module is not imported by production runtime code and has no broker/order adapter. Activation remains blocked on authenticated PAPER/account evidence, one-opening-order/one-stream binding, a trusted management-event queue identity, persistent protective/target-order semantics, runtime scheduling, and real PAPER evidence. | ADR-0035 |
| D-50 | **Accepted design (2026-08-25; owner-gated at merge): admit a filled QQQ PAPER opening through one default-off operation that accepts only its Chronos order reference and time, then derives all economic facts from durable intent/risk state and two stable canonical-broker reads.** Admission requires unchanged reconciliation provenance, positive execution identity matching local broker/permanent IDs, protected limit and cumulative terminal fill, no competing QQQ order, exact account-level QQQ quantity, and a named passing D-48 entry-risk check. Schema v11 atomically binds one opening order, deterministic managed stream, and broker permanent identity per account; exact retries rehydrate without another broker read. Multi-fill VWAP and derived CVaR values use explicit conservative 1e-8 persistence rounding. | Open-order disappearance cannot prove a fill, IBKR positions are account aggregates, and caller-shaped fill objects were not an authentication boundary. Positive broker evidence plus local lifecycle agreement and database uniqueness close the code-only registration gap without adding a runtime or mutation path. The seam remains unimported and cannot send, protect, schedule, or grant authority; ongoing management evidence, broker-held protection, scheduling, and real PAPER proof remain blocking. | ADR-0036 |
| D-51 | **Accepted design (2026-08-25; owner-gated at merge): add one content-addressed, authentication-only QQQ campaign-readiness compiler.** It pins the exact constitution, SMA control, Confluence candidate, inert PAPER management policy/source, and default-off opening-admission source; preserves both inherited blocker sets and zero-authority posture; and returns a typed ledger separating owner actions, Chronos build work, unavailable short evidence, and deferred activation work. The six-symbol QQQ robustness release and seven-symbol base Five-Tool companion intake remain distinct identities with evidence transfer forbidden. | The merged QQQ artifacts answer local questions but no single current artifact says what is implemented, what is proven, who supplies the next input, or which historical blocker has only been code-mitigated. A deep read-only seam makes that distinction deterministic without opening data or importing a registry, holdout, broker, order, supervisor, network, or promotion authority path. The inherited QQQ compilers do load inert Five-Tool market-data modules; the readiness operation never invokes or exposes their reader. It advances no gate and does not turn inert PAPER code into broker protection or real lifecycle evidence. | ADR-0037 |
| D-52 | **Accepted design (2026-08-26; owner-gated at merge): power the QQQ SMA-200 confirmatory cell on completed daily post-cost active returns, using a 4% minimum annual alpha, one-sided 5% type-I error, 80% power, 252 sessions/year, and an 8% annualized long-run tracking-error ceiling.** The prospective normal approximation requires 6,233 completed OOS daily returns (24.7302289281 year-equivalents). The 100-closed-position floor is a separate conjunct because sessions and positions are unlike units. The absolute earliest pass date remains `null`; a successor identity must bind both gates after the owner approves the clean-window start and covered session calendar. | “4% alpha” and “100 positions” alone cannot determine power. This identity makes the assumed dispersion, observation unit, multiplicity treatment, and dependence requirement reviewable before data exists, while refusing to fabricate a date or treat robustness cells/instruments as extra confirmatory observations. It reads no market data, registers no trial, unlocks no holdout, selects no strategy, and grants no order or promotion authority. | ADR-0038 |
| D-53 | **Accepted design (2026-08-26; owner-gated at merge): certification v3 binds a per-symbol semantic digest and distinct count for corporate actions inside the exact certified windows.** A positive sampled-action claim cannot exceed the distinct supplied events and cannot certify an all-empty panel; exact duplicates block. Legitimately action-free windows require a separate independent-source no-action attestation bound to the exact windows. The frozen six-symbol QQQ helper refuses an all-empty panel and validates manifest counts against parsed action bytes before receipt/declaration. | The prior typed sample count was not tied to the stream: six empty files could claim 12 samples, while cash distributions below the material-move threshold did not affect the certification digest. Content binding and typed empty-window evidence close that false-certification path without pretending code proves external completeness. No dataset, trial, holdout, strategy, order, funding, or promotion authority results. | ADR-0039 |
| D-54 | **Accepted and implemented (2026-08-29; owner authorized autonomous merge for this resumed sequence): operational health is one pure, conservative projection of an immutable fact snapshot, split into request liveness, operator-service readiness, and PAPER/LIVE/autonomous new-exposure capability.** Known negative evidence blocks; missing, stale, or future evidence is unknown and never strengthens a verdict. `/health` schema v2 preserves its seven legacy fields under `status_scope: compatibility_only`, polls only bounded local/cached observations, and never calls the broker. Dedicated `/health/live` and `/health/ready` endpoints expose the existing liveness/readiness verdicts as no-store HTTP 200/503 probe signals without exposing trading capability. Authority modules are structurally barred from importing the projection. | The old `status: ok` meant only that lifespan startup returned and could mask failed reconciliation or dead background tasks. Operators need a service to stay inspectable during broker, lease, or reconciliation failures without mistaking inspectability for permission, while orchestrators need status codes rather than a compatibility string. The projection grants nothing; no watchdog, SLO, external alert, orchestrator deployment, or operational-readiness claim results. | ADR-0040 |
| D-55 | **Accepted design (2026-08-29; observation-only continuation authorized by the owner): automatic clock health uses one fixed, bounded local `chronyc -n tracking` query and chrony's quantitative maximum-error bound.** Disabled is the default; enabling requires an explicit positive threshold. Only a fresh normal-leap, non-local observation at or below that threshold is `SYNCHRONIZED`; every missing, malformed, stale, future, local-reference, or uncertain result is `UNKNOWN`, while known not-synchronized or over-bound evidence is `UNSYNCHRONIZED`. Both backend roles sample into a generation-counted cache; health requests never execute the query. Schema v2 retains scalar `clock` and adds sanitized quantitative detail. Authority trees cannot import the observer. | A qualitative “NTP enabled/synchronized” Boolean cannot say how wrong the clock may be, while querying from `/health` would make polling an executable side effect. A fixed local provider, explicit owner threshold, documented error formula, bounded capture, freshness projection, and cache-only requests provide useful evidence without choosing risk appetite or granting authority. The code is fixture/process tested but this host lacks `chronyc`; no host synchronization, daemon management, actual submission clock gate, off-host alert, watchdog, SLO, or operational proof results. | ADR-0041 |
| D-56 | **Accepted design (2026-08-29; owner authorized autonomous merge for this resumed sequence): the release gate fails closed on three distinct security observations.** `pip-audit==2.10.1` audits the exact hash-locked runtime set with resolution disabled; `bandit==1.9.4` scans shipped Python, the out-of-package worker, and scripts at medium severity/medium confidence or stronger; `detect-secrets==1.5.0` checks every tracked file except its fingerprint-only baseline against explicitly reviewed false-positive fingerprints. Exact tool versions live in the dev lock. The wrapper refuses version drift, an empty tracked set, scanner failure, or a baseline that its copied non-mutating check would rewrite. | The validated SBOM made the runtime inventory inspectable but did not judge it. A baseline probe found twelve current advisories against transitive runtime GitPython 3.1.52 and a fixed shared `/tmp` path; upgrading GitPython to 3.1.61 and making research halt state privately temporary cleared those findings. These scans are current advisory/heuristic evidence, not proof of non-malicious dependencies, complete secret history, low-confidence static cleanliness, reproducible wheel bytes, or a signed artifact. They grant no runtime or trading authority. | ADR-0042 |
| D-57 | **Accepted design (2026-08-29; owner authorized autonomous merge for this resumed sequence): release-wheel reproducibility is an observed two-build invariant tied to exact Git source identity.** The release gate derives `SOURCE_DATE_EPOCH` from the repository `HEAD`, overrides ambient caller input, copies the same source set into two isolated source/output trees, and runs the exact hash-locked setuptools backend twice. Publication requires one matching filename, byte-for-byte equality, and the source-derived UTC timestamp on every ZIP member after setuptools' pre-1980 clamp and ZIP's two-second precision. | Locks and a reproducible SBOM do not prove the wheel itself is deterministic. The explicit second build makes time- or path-dependent wheel drift observable and fail-closed while preserving the existing installed-artifact and SBOM checks. This proves same-revision repeatability in the gate's pinned local/hosted environment, not signing, independent rebuilding, cross-platform identity, provenance against a malicious builder, or any runtime/trading authority. | ADR-0043 |
| D-58 | **Accepted design (2026-08-29; owner authorized autonomous merge for this resumed sequence): every supported build path installs and independently verifies one exact hash-locked pip frontend before any other dependency operation.** The interpreter's offline `ensurepip` bundle remains the initial trust root; it may perform only the self-replacement from `requirements-bootstrap.lock`. CI and both release-artifact venvs then require the installed pip distribution to match that lock, and the runtime SBOM requires the same exact pip component with no unlocked environment extras. | The backend, dependency closures, and SBOM generator were locked, but an unbounded CI upgrade and interpreter-selected release frontend could change how every lock was consumed. A dedicated bootstrap domain closes downloaded pip artifact/version ambiguity without pretending a lock authenticates the interpreter, publisher, or builder. It grants no runtime or trading authority. | ADR-0044 |
| D-59 | **Accepted design (2026-08-29; owner authorized autonomous merge for this resumed sequence): the release gate scans every detector-recognized addition in the exact Git ancestry reachable from candidate `HEAD`, including root commits, deleted files, NUL-bearing files, and two-parent merge-resolution deltas.** Hosted CI fetches complete history; the gate rejects shallow history and octopus merges, forces every blob diff through Git's text patch path, bounds the patch stream at 128 MiB, confines remerge scratch objects outside the repository, applies the pinned detect-secrets policy through exact unidiff parsing, and emits only path/type/line/fingerprint metadata. Seven historical SHA-256 identities are explicitly reviewed by exact fingerprint and stale exceptions block. | A clean tip does not make a deleted credential unrecoverable, and ordinary `git log -p` both omits merge diffs and suppresses hunks when Git classifies a blob as binary. Complete, tested traversal closes those selection gaps without weakening detector policy or storing candidate plaintext. This remains heuristic evidence over `HEAD` ancestry, not credential rotation, history rewriting, push protection, signing, or proof against unrecognized or arbitrarily encoded secrets. It grants no runtime or trading authority. | ADR-0045 |
| D-60 | **Accepted design (2026-08-29; owner authorized autonomous merge for this resumed sequence): successful exact-main CI creates one Sigstore-signed GitHub build-provenance attestation over the validated wheel and CycloneDX SBOM.** A downstream job runs only for a push to `main`, after `quality`, downloads the same-run retained artifact with digest-mismatch refusal, and passes only the two exact release path families to SHA-pinned `actions/attest`. The OIDC and attestation write permissions exist only in that job; PR and quality jobs retain read-only repository access. | Reproducible bytes and a checked SBOM did not let a consumer prove which repository workflow produced downloaded files. Binding both digests to GitHub's short-lived workflow identity closes that provenance gap without granting the builder package-publication, repository-write, runtime, or trading authority. The attestation proves origin and integrity, not artifact safety, publisher identity outside GitHub, independent reproducibility, package-native signatures, or resistance to a compromised workflow/runner/action. | ADR-0046 |
| D-61 | **Accepted design (2026-08-29; owner authorized autonomous merge for this resumed sequence): external health observation is a default-off one-shot client of the exact liveness/readiness status-code contract, never a new authority input.** It accepts only a plain credential-free HTTP(S) origin, ignores environment proxies, refuses redirects, applies a positive finite HTTPX network-inactivity timeout per endpoint, consumes no response body, and emits closed `HEALTHY`/`UNHEALTHY`/`UNKNOWN` JSON plus machine exit status. Authority trees are structurally barred from importing it. | The in-process health projection cannot prove that its own process or route is reachable, but inventing a scheduler, remote exposure, alert destination, or dead-man policy would cross operational ownership boundaries. A constrained observer gives an external scheduler a safe primitive while honestly retaining every deployment, outer-deadline, silence-detection, alerting, SLO, and transport residual. | ADR-0047 |
| D-62 | **Accepted design (2026-09-03; owner-gated at merge): every registry-authenticated proposal and evidence bundle is durably bound at enqueue or issuance to the credential epoch (`secret_sha256`) and a canonical SHA-256 of the complete validated registration.** At drain, Chronos may stamp and bind only the same still-present, enabled, unexpired, unrevoked registration; same-id credential rotation, expiry renewal, identity-label edit, or later registry removal refuses rather than adopting or downgrading queued work. Pre-0011 rows are preserved with NULL bindings and are never inferred from mutable current configuration. | `proposer_id` is a reusable label, not an immutable authentication fact. Exact-grant binding prevents revocation, rotation, or configuration removal from reviving and relabelling queued authority while preserving ADR-0023's credential-derived identity and ADR-0028's backend-owned evidence record. | ADR-0048 |
| D-63 | **Accepted design (2026-09-03; owner review required before merge): a live-safety state file that is missing *after this installation wrote one* fails closed.** The state directory carries an installation marker (`chronos.orders.state_generation`) in which each component records its first durable write. A kill-switch file missing under a marker that names it reads ENGAGED; a session baseline missing under the same condition refuses and engages the switch instead of re-baselining at the post-loss net liquidation value. Absence with no marker remains a fresh install and keeps the permissive reading, so a new deploy still boots trading-capable subject to the other gates. Closes the kill-engaged half of VISION §6 finding 3; read-only and unreconciled on restore, and ADR-0017 mandate auto-activation, remain open. ADR-0049, R-66. |
| D-64 | **Accepted design (2026-09-03; owner review required before merge): a file that grants authority is read through one descriptor, and an unsafe one is refused rather than repaired.** `chronos.utils.secure_files.read_authority_file` opens with O_NOFOLLOW and O_NONBLOCK, then establishes on the open descriptor that the file is regular, owned by this effective user, exactly 0600, and singly linked, before reading the bytes it digests; `create_authority_file` uses O_CREAT|O_EXCL at 0600 so a new credential is never briefly world-readable. Wired first to the local API token (`chronos.api.auth`); the owner-authored mandate and proposer registry follow in a separate PR. Widening or narrowing a credential's mode on the operator's behalf is explicitly rejected: startup fails with a CRITICAL line naming the file and the fix, because a silent chmod hides that something put the file in that state. R-67. |
| D-65 | **Accepted design (2026-09-04): the model worker may think on a local OpenAI-compatible server — loopback-only, key optional, and with no default model tag.** `CHRONOS_WORKER_PROVIDER=local` selects a raw-httpx Chat Completions transport (`worker/model_local.py`) that forces `propose_decision` and mirrors the xAI path's deny-by-default extract. `CHRONOS_WORKER_LOCAL_BASE_URL` defaults to `http://127.0.0.1:11434/v1` and startup refuses a non-loopback host or non-http(s) scheme; `CHRONOS_WORKER_LOCAL_API_KEY` is optional, and unset means no `Authorization` header is sent at all; `CHRONOS_WORKER_MODEL` is required for this provider and startup fails loud without it. | The first SHADOW rung is a long run of cheap cycles and both existing providers are metered — a bill attached to an evidence campaign is a standing reason to shorten it, and the hardware already hosts loopback inference. The three constraints exist because a local endpoint is *configuration* where the hosted ones are source constants: the request body carries the whole evidence snapshot, so a redirectable URL is an exfiltration path; a local server usually authenticates nothing, so a mandatory key would be ceremony and a placeholder worse; and a guessed roster tag is either absent or a different model than the operator believes is thinking. Grants no trading authority — `CHRONOS_WORKER_FORWARD` still defaults false. | ADR-0050 |
| D-66 | **Accepted design (2026-09-04; owner-gated at merge): a mandate in a submitting mode (PAPER_AUTONOMOUS, CANARY_LIVE_AUTONOMOUS, LIVE_AUTONOMOUS) assembles only on the authenticated provenance posture — a proposer registry (ADR-0023) AND evidence binding (ADR-0028), together.** The cap runs at assembly before activation (no activation row is written), notes the typed startup fault `autonomy_posture_unauthenticated`, raises a CRITICAL owner alert, and is a BLOCKING preflight finding. The unset posture is unchanged for SHADOW and every non-submitting mode. | On the static posture the stamped identity is the ingress placeholder and admission check 9 compares a constant against itself; the 2026-09-03 review admitted an arbitrary evidence citation under a PAPER mandate that way (P1-05). A decision that can reach the wire must not be admitted on an identity nobody authenticated and evidence nobody issued; capping the compatibility posture to SHADOW keeps it exactly where it is SHADOW-grade. | ADR-0051 |
| D-67 | **Accepted design (2026-09-04; owner-gated at merge): a cycle with a configured order-plane handoff spends the mandate's `orders_submitted` and turnover BEFORE the handoff and commits that spend through an injected `commit_before_handoff` seam; a configured handoff without the seam refuses `NO_DURABLE_RESERVATION` and transmits nothing; the reservation is released only for `REFUSED_NOT_SENT`, the one disposition that proves nothing left the process.** SHADOW (no `submit`) is unchanged. | The attempt was recorded after the handoff inside the tick's transaction, so a crash between the broker answering and the supervisor commit rolled the spend back and let a different decision after restart re-spend an allowance the venue may already hold (P1-02, measured 2026-09-03). Reserving from a second session deadlocks — the cycle holds an open SQLite write transaction and production runs WAL, one writer — and an in-memory database hides that because StaticPool shares one connection, so the split of the cycle's own transaction is the only repair that survives the seam. | ADR-0052 |
| D-68 | **Accepted design (2026-09-04; owner review required before merge): authority files are read under one of two named contracts, and the caller must say which.** `AuthorityMode.SECRET` (the local API token) keeps ADR-0052's exact-`0600` rule. `AuthorityMode.GRANT` (the owner-authored mandate and the proposer registry, which now adopt the descriptor-bound helper) requires only that no group- or other-**write** bit is set: `0600` and `0644` accepted, `0664`/`0666`/`0777` refused. Every non-mode check — regular file, `O_NOFOLLOW`, effective-owner uid, single link, same-descriptor read and digest — is identical in both. There is no default mode. Unsafe files are refused with a CRITICAL line naming file, mode, contract and fix, never repaired; `UnsafeAuthorityFile` stays distinct from "invalid document" and reaches startup as `StartupFaultCode.AUTHORITY_FILE_UNSAFE`. UTF-8 decoding moved into the read, so a non-text authority file refuses instead of raising `UnicodeDecodeError` out of startup. | The token's contents ARE the credential, so reading it is the compromise and `0600` is right. A mandate is an owner-authored decision that discloses nothing when read and the registry stores only hashes — what matters there is that nobody else could have WRITTEN it. Applying the token's rule to grants would have refused every mandate `chronos.cli mandate template > file` produces (a shell redirect takes its mode from the umask: `0644` under `022`, `0664` under the Debian/Ubuntu `002`), turning a documented setup step into a startup refusal. Accepting group-writable instead would leave the grant rewritable by anyone in the group, which is the threat. Grants no trading authority; the mandate remains the only grant and this narrows who may author it. | ADR-0053 |
| D-69 | **Accepted design (2026-09-04; owner review required before merge): a boot that cannot prove it does not follow a restore comes up read-only and unreconciled.** The state directory's installation id is recorded in a second, independent durable store — the Chronos database — and the two are compared at every writer startup. A marker missing while the database remembers one, a database with no memory of a surviving marker, two stores naming different installations, and an unreadable marker each raise a recovery hold. A hold refuses every writer route and the submission boundary itself, leaves reconciliation readiness `PENDING` with no refresher task, and does not build the autonomy runtime — so ADR-0017's mandate auto-activation cannot re-arm a restored grant. The writer lease is deliberately kept and heartbeated. An operator clears one observation with a typed note that binds that exact observation, effective at the next start. | The marker of ADR-0049 is a single store, so the absence of the marker and the absence of everything are the same bytes; a second store is the only thing that can tell a lost state directory from a fresh install. Dropping the lease instead would hand write authority to the next process to start, which reads the same unverified state. Refusing to activate is not revoking, so acknowledging costs the owner no `mandate_version`. **Residual (R-72):** the kill-switch file and the database live in one directory by default, so a self-consistent wholesale restore carries both witnesses from one snapshot and is indistinguishable from a clean restart; `chronos.recovery restore` now leaves an outside witness, and the manual restore step remains the control for a hand-made restore that omits it. | ADR-0054 |
| D-71 | **Accepted design (2026-09-05; classification stated in the PR): the autonomy SHADOW campaign runs on the AUTHENTICATED proposer posture — a proposer registry (`AUTONOMY_PROPOSERS_FILE`) and evidence binding (`AUTONOMY_EVIDENCE_BUNDLES`) — even though a SHADOW mandate does not require either.** The campaign's credential is minted with an expiry past the campaign's end, or its rotation is a recorded mid-campaign event. A run with `CHRONOS_WORKER_FORWARD=false` is a worker-stability campaign and is never counted as promotion-ladder rung 1. | Three reasons that converge on one: run the campaign on the posture you intend to promote into, or ninety days later the evidence describes a configuration that can never ship. ADR-0051 refuses to assemble a PAPER- or LIVE-capable mandate on the static posture, so the posture would change underneath the owner on promotion day. On the static posture admission check 9 compares the single `INGRESS_IDENTITY` constant against itself and cannot refuse, so a campaign that "exercised" it exercised nothing. ADR-0048's credential-epoch binding has nothing to bind without a credential. The cost is one `proposer mint` and one environment variable. This grants no runtime or trading authority and starts nothing: the runbook and templates are documentation, and `*_FORWARD` remains an owner-only act. | docs/SHADOW_CAMPAIGN.md |
