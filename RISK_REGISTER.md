# RISK REGISTER

Living register of project and operational risks. Severity: C=critical, H=high,
M=medium, L=low. Status: OPEN / MITIGATED / ACCEPTED / CLOSED.

| ID | Risk | Sev | Status | Mitigation / Notes |
|----|------|-----|--------|--------------------|
| R-01 | Accidental live trading | C | MITIGATED (posture restated 2026-07-25) | **The blanket "no live-capable code path" claim is retired — it described the pre-M7 build.** Today: the deterministic platform's mode lock still hard-denies CANARY_LIVE/LIVE (ADR-0007, untouched by ADR-0016), but the `chronos.orders` plane *is* live-capable behind ADR-0009's nine-conjunct configuration, the ten-gate stack, arming, the durable kill switch, the drawdown breaker, and the writer lease. Under ADR-0016 live *autonomous* operation will additionally require an active owner-authored mandate — **not yet an enforced control**: no autonomy startup path, mandate store, or gateway exists (M2), so today the only thing standing between configuration and a live order is the ADR-0009 stack above. `DEFAULT_AUTONOMY_MODE` is `SHADOW` as a contract constant with no reader yet. No test, CI run, or dev path can transmit. Re-verify at every release (tests/safety). |
| R-02 | Paper/live confusion (wrong account) | C | MITIGATED | Account pattern `D[UF]\d{4,}` + operator allowlist + broker-reported environment + exact managed-accounts match before every submission window. Tested. |
| R-03 | Duplicate orders (replay, reconnect, double signal) | H | MITIGATED | Deterministic intent ids (UUIDv5 of economic content); ledger primary-key refusal; risk-engine duplicate check; sim-broker duplicate rejection; idempotent event application. Tested. Residual: cancel-replace flows are not implemented (no modify path exists). |
| R-04 | Unknown broker state after disconnect/restart | H | MITIGATED | Startup/reconnect enters non-trading state; reconciliation must pass; unknown order/position blocks trading and halts; no auto-flatten. Residual: reconciliation evidence collection against a real gateway is untested here (owner action). |
| R-05 | Risk-engine bypass by strategy code | H | MITIGATED | Structural: proposals cannot express orders; approvals are instance-token-bound; frozen policy; engine exceptions deny. Tested (forged approval, foreign engine, policy mutation). |
| R-06 | Halt cleared by restart | H | MITIGATED | Halt state is a durable file; missing/corrupt file reads as HALTED; rearm requires operator note. Tested. |
| R-07 | Look-ahead / repainting contamination in translated strategies | H | MITIGATED (for derived cores) | Closed-bar-only engine; next-bar fills; audit of every script (docs/PINE_AUDIT.md); derived strategies re-specified rather than trusting Pine defaults. Residual: no TradingView reference exports — parity is spec-level only (A-03). |
| R-08 | Research data provenance (public mirrors, not broker data) | H | OPEN (ACCEPTED for research phase) | Per-file manifests with hashes + validation + cross-checks; research conclusions carry a data caveat; owner should re-run against IBKR historical data before promotion beyond paper. **Broadened universe is heterogeneous**: SPY/QQQ unadjusted & byte-exact; IWM/TLT dividend-ADJUSTED, GLD nominal, all three markdown-transcribed (2-decimal) for 2019–2021 only (validation-window-only). Cross-symbol absolute-return comparisons are not apples-to-apples; each symbol is judged within-symbol. DIA not acquired. The re-run did not change the verdict (zero selected); disclosures in docs/RESEARCH_REPORT.md and research/selection_manifest.json. |
| R-09 | Overfitting / selection bias in strategy choice | H | OPEN | Chronological partitions, untouched final test window, baselines incl. deterministic random twin, parameter-stability review; small candidate count. Residual risk inherent to backtesting is documented in RESEARCH_REPORT. |
| R-10 | Small-account economics (commission drag, whole shares) | H | ACCEPTED (staged 2026-08-25) | The last documented account snapshot remains ≈ **USD 110**. For QQQ v1, **USD 3,000 is a research reference and conditional future funding target, not a funded or live balance**; current live allocation and live risk are USD 0. Funding requires an untouched-holdout pass, at least 90 days of shadow evidence, supervised-paper evidence, and fresh owner approval, and still grants no trading authority. The frozen USD 2.10 round-trip commission floor, 2 bps/side base slippage, whole-share rounding, and short-side borrow costs can invalidate marginal edges. No sizing, selection, promotion, or short-side work may silently assume funding, borrowing, or eligibility. |
| R-11 | Intraday corpus and companion validation incomplete | M | ACCEPTED (premise narrowed 2026-08-24) | ADR-0029/D-32 provides an hourly ingestion + certification path, but no certified hourly release or intraday validation plan exists and `docs/TEST_PLAN.md` still excludes intraday. The 2026-08-24 companion screen also identifies a separate evidence gap: the highest-priority future candidate (arrival relative spread + quote freshness) requires certified identity-bound NBBO/trades, exact admission timestamps, and a content-addressed cost schedule that hourly bars cannot supply. The causal arrival-quote module closes only an alignment-code blocker; it selects no feature, opens no data, freezes no threshold, and runs no trial. Executable intraday trading remains out of scope on D-12 account-economics grounds. Re-state after both a certified release digest and a reviewed intraday campaign identity exist. |
| R-12 | ib_async maintenance / TWS API changes | M | OPEN | Pinned range in pyproject; ADR-0002 records alternatives; adapter surface is narrow (IBLike protocol) for replaceability. |
| R-13 | SQLite durability under crash | M | MITIGATED | WAL + synchronous=FULL; ledger failure halts trading before submission; backup/restore documented. |
| R-14 | Audit-log tamper or corruption | M | MITIGATED | Hash-chained records, fsync on append, verify command, incident playbook. A corrupt final line now raises a specific `AuditLogCorruptionError` on construction (fails closed) rather than an uncaught exception (independent review H4). |
| R-20 | Halt bypass by a halt landing mid-submission (TOCTOU) | H | MITIGATED | The execution engine re-reads the halt immediately before handing the order to the broker, not only at the top of the call (independent review H1). Regression-tested. |
| R-21 | Platform state files world-readable on a shared host | M | MITIGATED | Ledger/halt/audit files (and WAL/SHM sidecars) are chmod 0600 via a symlink- and ownership-checked helper; halt write is fsync-durable (independent review M1/L1). |
| R-22 | Reconciliation is presence-only (no state-level contradiction detection) | M | MITIGATED | `reconcile()` now takes optional per-order state maps and reports `ORDER_STATE_MISMATCH` when a broker/ledger order disagrees on (lifecycle bucket, filled qty); the service startup (`chronos.service.startup`) supplies both sides and halts on any discrepancy. No auto-flatten. Tests: `tests/platform_unit/test_reconciliation.py`, `test_service.py::test_state_mismatch_halts_and_blocks`. |
| R-23 | Restart does not hydrate in-flight orders; their broker events halt and lose the evidence trail | M | MITIGATED | `ExecutionEngine.hydrate_from_ledger()` (fed by `ledger.working_order_snapshots()`) rebuilds in-flight orders at startup so a post-restart broker event applies to a real state machine and is recorded instead of triggering an evidence-losing `UNKNOWN_ORDER` halt. Tests: `tests/platform_unit/test_hydration.py`. |
| R-15 | Dependency supply chain (no lockfile) | M | MITIGATED | A hash-verified lockfile (`requirements-dev.lock`, full transitive closure with SHA-256 pins) is committed and generated from pyproject via `uv pip compile --generate-hashes`. CI and the documented deploy path (docs/DEPLOYMENT.md) install with `pip install --require-hashes -r requirements-dev.lock` then `pip install -e . --no-deps`. Residuals (M5 review): the PEP 517 build backend (`setuptools`/`wheel`) and pip itself are fetched outside the hash gate, and the owner must regenerate + review the lock diff when bumping a bound (docs/SECURITY.md). The `aeventkit` entry was verified as ib_async's genuine declared dependency, not a typosquat. |
| R-16 | Notion corpus drift vs audited hashes | L | MITIGATED | Registry pins SHA-256 per script; `verify-corpus` command detects drift. |
| R-17 | Wheel dashboard and platform state confusion | L | MITIGATED | Separate database files and schemas (ADR-0003); no shared mutable state. |
| R-18 | Clock drift on operator host | M | OPEN | Staleness checks bound decision ages; time-sync check is documented operator duty (docs/OPERATIONS.md); automated NTP verification not implemented. |
| R-19 | Session usage limits interrupting long autonomous builds | L | ACCEPTED | Workflow resume caching + checkpoint commits made recovery cheap (observed and exercised during this build). |
| R-24 | Writer lease is never renewed in production, and is not a fencing token | C | MITIGATED (M2, 2026-07-25) | **Fixed.** The backend lifespan now runs a heartbeat that renews the lease at TTL/3 and, on any renewal failure, permanently demotes the process to read-only (re-acquiring would be unsafe — another writer may already have acted). `WriterLease.holds()` re-checks ownership **in the database** and the submission boundary calls it immediately before the transmit line, alongside the kill-switch re-read; a refusal there is provably not-sent. Tests: `tests/safety/test_writer_lease_fencing.py` (7), incl. structural guards that the lifespan actually renews/binds and that the re-check stays adjacent to the transmit line. **Residual (disclosed):** this narrows the window, it does not close it — IBKR accepts an order without knowing about our lease, so true broker-side fencing is unavailable and a sufficiently unlucky pause between the check and the wire cannot be defended from here. Original defect below. |
| R-24-orig | (historical detail of R-24) | C | CLOSED | `WriterLease.renew()` has no production caller (`grep '\.renew('` over `src/` is empty), so the 30-second lease silently expires mid-session while `BackendState.writer` stays True; a second backend can then acquire it while the first still believes it holds it. The lease token is never passed to or checked by DB writes or the broker send, so it is an advisory lock, not a fencing token. Harmful today; **unacceptable under unattended autonomy**, where no human is watching for the split-brain. Found by the M0 audit; must be fixed in M2 before any autonomous submission. |
| R-25 | `max_opening_orders_per_day` is inert | H | MITIGATED (M10, 2026-07-27) | **The cap is wired and, for the first time, capable of refusing.** Two independent defects each kept it inert: `gather` never set `opening_orders_today` (so the field's `0` default made `0 + 1 <= limit` true on every call, forever), and `count_opening_since` — which had **zero callers** — additionally filtered `action == SELL`, and `OPEN ∧ SELL` is exactly `{OPEN_SHORT_PUT, OPEN_COVERED_CALL}`, so every stock and crypto opening was invisible to the cap even once wired. Both are fixed: the side filter is gone (`open_close_effect` is the only predicate that means "this creates exposure"), and the provider takes the count through a structural `_OpeningCounter` protocol so `chronos.orders` still does not import `chronos.persistence`. **The day is the market's, not UTC's** — 21:00 New York is already tomorrow in UTC, so a UTC boundary would hand out a second full allowance every evening, and crypto trades 24/7. **Counted at creation, not at fill:** the limit exists to bound an unthrottled decision loop, and a loop that could mint a thousand refused intents without moving the counter would be bounded by nothing. `RiskEvidence.opening_orders_today` is now `int | None` defaulting to `None`, and an uncountable day is **UNKNOWN → blocked**, not a passing zero: a cap that reports full headroom precisely when it cannot see has stopped existing. Closing intents are never capped — throttling the orders that reduce exposure would be backwards. ADR-0010 §4 claimed this was wired; that claim is corrected at its source. Tests: `tests/safety/test_opening_cap_exercised.py` (14), each half of the fix verified by reverting it and confirming a distinct failure. **Residual (disclosed):** the count is per-process-read and not transactionally fenced against the write, so two backends racing the same allowance could each see the pre-write count — R-24's writer lease is what makes that a single-writer question, and it carries its own live residual. |
| R-26 | `market_open` is permanently AMBIGUOUS in production | H | MITIGATED (M9, 2026-07-26) | **The gate is supplied and, for the first time, exercised.** `BrokerRiskEvidenceProvider._broker_confirms_open` hard-returned `None` from M5 until now, so every equity and option instant inside regular trading hours resolved to AMBIGUOUS and blocked. Fail-closed, so never a live-money hazard — but it meant **no live equity or option order could pass risk at all** and the session gate had never once been observed saying OPEN. Closed by reading IBKR's own `liquidHours`/`timeZoneId`, which were arriving on **every** qualification and being dropped one attribute short of the code that needed them (they live on `ContractDetails`, not on the `Contract` inside it — `instrument_from_contract` never saw them). `chronos.services.liquid_hours` parses both IBKR formats, the `;`/`,` separator vintages, overnight windows, and `2400` closes; `UnderlyingContract`/`OptionContract` carry the evidence; the provider reads it off `intent.contract`, so the answer costs **no broker round-trip**. **The load-bearing token is `CLOSED`** — that is the venue telling you a normal-looking Friday is not a trading day, which is precisely the fact a weekday-and-clock calendar can never derive. Every failure degrades toward blocking: unresolvable timezone, unparseable string, a day the schedule never covered, or a contract with no hours all return `None` → AMBIGUOUS. A spurious `True` is the only output in the chain that could open a gate that should hold, and the tests are weighted accordingly (13 malformed-input cases vs 5 happy-path). **Residual (disclosed):** the parse is verified against fixtures, not a live gateway — real `liquidHours` strings from a running TWS remain an owner verification item, as with every other adapter path (R-04). Tests: `tests/safety/test_liquid_hours.py` (29), `tests/safety/test_session_gate_exercised.py` (9). |
| R-27 | Option deliverable verification comes only from `DemoBroker` | H | MITIGATED (M11, 2026-07-27) | **Both IBKR adapters now screen the deliverable, and the check passes for the first time.** `deliverable_verified=True` had exactly one setter — `DemoBroker`, by fiat — so `standard_deliverable_verified` FAILed every option order against a real gateway and the whole option path was unproven outside demo. The stake is the assignment math: a short put's obligation is `strike × multiplier × contracts`, which is true only for a **standard** contract, and an OCC-adjusted series delivers something else (150 shares, shares plus cash, another issuer's stock), so sizing one as if it delivered 100 reserves the wrong number in the direction that leaves the account short at assignment. `chronos.services.option_deliverable` screens each qualified option on five **necessary, conjunctive** conditions: the broker named the underlying contract, the underlying is `STK`, the underlying symbol is the option root, the OCC root still equals the symbol, and the multiplier is 100. Same failure mode as R-26 — `underConId`/`underSymbol`/`underSecType` live on `ContractDetails`, not on the `Contract` inside it, so nothing downstream had ever seen them. A contract that fails the screen is returned **unchanged**, i.e. exactly the pre-M11 state, so failing is never worse than not having run it. One deliberate asymmetry: an unparseable local symbol is *not* held against the contract (the OSI root carries no information the trading class does not, and refusing over an unverified cosmetic field would make this control inert the way R-25 and R-26 were), but a local symbol that parses and **contradicts** the OCC root refuses. Tests: `tests/safety/test_option_deliverable.py` (30), plus `tests/unit/test_ibkr_broker.py` where a line that asserted `deliverable_verified is False` for six milestones now asserts `is True`; each condition verified by reverting it and confirming a distinct failure. **Residual (disclosed, and the reason this is MITIGATED rather than CLOSED):** the TWS API does not expose OCC's deliverable schedule — no field says how many shares of what a contract delivers. This infers the *absence of an adjustment* from OCC's root-naming convention. It is a non-standard **detector**, not a deliverable **reader**, and like every other adapter path it is verified against fixtures rather than a live gateway (R-04). |
| R-28 | Dormant second submission path in the deterministic plane | H | MITIGATED (M2, 2026-07-25) — QUARANTINED | **Quarantined, not retired** (it keeps its tests and history). `IBKRPaperExecutionAdapter` now refuses construction unless passed `quarantine_ack=True`, which **no** module in `src/` passes — so a future wiring mistake fails loudly at construction instead of quietly acquiring a second, ungated broker path. Added `tests/safety/test_broker_mutation_inventory.py`: a **repository-wide** inventory of every transmit-enabling site matching *both* spellings (keyword `transmit=True` **and** attribute `order.transmit = True` — the dormant site is an attribute assignment, which the older `chronos.orders`-scoped keyword test structurally could not see), pinned to an explicit expected set so any new site anywhere fails CI; plus an AST assertion that no production module constructs the adapter. Original defect below. |
| R-28-orig | (historical detail of R-28) | H | CLOSED | `chronos/execution/brokers/ibkr_paper.py` holds a working `placeOrder`/`cancelOrder` with a **hardcoded** `order.transmit = True` (:120) that the single-transmit-site AST test does not scan, and a `request_cancel` that re-verifies nothing. It is constructed nowhere in production (the only `ExecutionEngine` wiring passes `NullExecutionBroker`), and its docstring's "ONLY code path" claim is stale. One wiring change would create a second transmit plane with none of the ADR-0009 gates. M2 must retire it, quarantine it, or prove its isolation structurally. |
| R-29 | Autonomous model authority materially expands risk | C | OPEN (ACCEPTED, owner-directed) | ADR-0016/D-16 create the first code path by which a non-deterministic component can originate an order. Mitigations are structural and layered: one decision type that cannot express an order, one gateway, one transmit site, model isolation outside the broker-writing process, an expiring deny-by-default owner mandate, per-family promotion, and an unconditional deterministic veto. Residual risk is real and accepted by explicit owner directive; it is bounded by mandate limits, the kill switch, and the drawdown breaker, and reduced further only by the M2-M10 gates. |
| R-30 | Prompt injection into model decisions | H | MITIGATED (M4, 2026-07-25) — bounded, not solved | Chronos does **not** claim to prevent prompt injection; nothing does, reliably. The claim it makes is narrower and testable: **a successful injection cannot become a trade the mandate would not otherwise have permitted.** External text arrives as `TextualEvidence`, which cannot be marked trusted, is never parsed by any deterministic module (AST-asserted), and is carried verbatim rather than sanitized — stripping it would destroy the record of what the model was actually shown. The tool surface has no write kind at all: `ToolKind` is `{READ, DECISION}`, handlers receive the evidence bundle and nothing else, the registry freezes at startup, and unknown tool names refuse rather than falling through to a near match. Injection tests assert that adversarial narrative changes no compiled order parameter byte-for-byte, does not change the decision id, and that an injected size is still clamped by the mandate. **Residual (disclosed):** an injection can still cause a differently-shaped *proposal*, which is then judged like any other — that is the design, not a gap. Process-level model isolation (R-35) is a separate, open item. |
| R-31 | Model repeatedly re-submitting a refused decision | M | MITIGATED (M2, 2026-07-25) | **Partly enforced.** `chronos.supervisor.admission` tracks both halves: an already-**admitted** decision id is refused as `DECISION_REPLAY`, and a **refused** one is counted, with `MAX_RESUBMISSIONS = 3` attempts before `RESUBMISSION_EXHAUSTED` refuses it permanently — so a refusal cannot be routed around by repetition. An earlier revision of this row said "enforcement lands with the gateway in M2" after the gateway had shipped, and the M2 review also found only admitted ids were tracked; both are corrected. **Durable since M3 (2026-07-25):** the counters live in `autonomy_decision_attempts` and are rebuilt by `supervisor.durable.build_state`, so a restart no longer resets the attempt budget — patience does not defeat the bound. **Closed in M4 (2026-07-25):** deduplication is no longer by a model-chosen id. `chronos.supervisor.queue` withholds `decision_id` from `ProposedDecision` entirely and derives it as a UUIDv5 over the proposal's **economic content**, so re-proposing the same trade yields the same id and is caught as a replay. Narrative is excluded from the digest, so rewording a thesis does not mint a fresh id and hand back the retry budget. **Remaining bound (accepted, not a hole):** a model can still get a fresh budget by proposing a genuinely *different* trade — a different size or strike. That is correct, because a different trade is a different decision and faces every gate again, including the mandate ceilings that bound total attempted exposure. Tests: `tests/safety/test_supervisor_gateway.py`. |
| R-32 | Owner alerts have no out-of-band delivery channel | H | MITIGATED (M6, 2026-07-25) — local channels only | **Delivery exists.** `chronos.supervisor.delivery` pushes unacknowledged alerts to sinks, records `delivered_at` so *told* is a different fact from *acknowledged*, and counts attempts durably so a persistently failing sink is visible rather than silently retried. Shipped sinks are a always-present log sink and an optional JSONL file sink (0600, fsync'd, `O_NOFOLLOW` per R-21) that an operator's own tooling can watch — `tail -f`, an editor watch, a systemd path unit, a desktop notifier. An alert counts as delivered when **at least one** sink accepts, so a misconfigured file path cannot suppress the log sink forever; a sink that raises is caught, because an alerting system that crashes the process it is alerting about has made things worse. `GET /autonomy/alerts` remains on the pull side. **Deliberately local-only, enforced by a structural test that fails on any network import:** a networked sender needs credentials beside a process that moves money and an egress path a compromised component could ride, and its failure mode is silence — which converts "no alerts" from *unknown* into *all clear*. **Residual (disclosed):** a local file does not follow you off the machine. Reaching a phone needs a networked channel and its own ADR, so for genuinely unattended operation away from the host this remains open. |
| R-33 | Hash chaining is tamper-evident, not tamper-proof | M | ACCEPTED (disclosed) | `chronos.persistence.hash_chain` gives the database's append-only tables the property the JSONL audit log has had since Phase 9: a targeted edit, deletion, reordering, or partial corruption breaks verification. It does **not** stop an attacker who can write to the database from recomputing every subsequent digest and presenting an internally consistent chain — no hash chain stored in the file it protects can, and the JSONL log has the same bound. Defeating a full rewrite needs an external anchor (an offsite copy of the head digest, or signing), which Chronos does not have and does not claim. Accepted because the realistic threat is corruption and casual edit, both of which are now detected. |
| R-34 | Session boundaries are calendar dates, not market sessions | M | MITIGATED (M5, 2026-07-25) | `supervisor.durable.session_key` accepts an optional `market_timezone` and converts before taking the date, so the counter rolls where the market's own day does rather than at UTC midnight — the defect being that 22:00 in New York is already the next UTC day, so a single trading afternoon straddled two counters. It is **optional and explicit**: defaulting to a guess would invent the boundary every loss and activity limit is measured across, and invent it wrongly for anyone trading elsewhere. An unknown zone **raises rather than falling back to UTC**, because a silent fallback is wrong in exactly the way nobody notices. `CycleFacts` carries it through the loop. **Residual (disclosed):** this aligns to a market's *calendar day*, not its session calendar — no holidays, no half-days, and an overnight futures session still spans two dates. For equities and equity options, the whole of the current capability matrix, a market-local day and a session coincide. Futures would need a real calendar and are not tradable. |
| R-35 | Model isolation is a code boundary, not a process boundary | H | MITIGATED (M5, 2026-07-25) | **The inversion is the fix: Chronos does not call a model — a model worker calls in.** `supervisor.ingress` accepts a JSON `ProposedDecision` from a worker running in a separate process (the owner's own AI workspace, for example). Consequently there is no provider SDK, no API key, and no egress path in the broker-holding process, and a worker that dies or is never started simply produces no decisions. The worker holds no Chronos capability — no broker handle, no session, no lease, no kill switch, no submission path — not by promise but because none was ever in its address space. The ingress treats every payload as hostile: bounded size before parsing, strict single-object JSON, NaN/Infinity refused (`NaN > limit` is False, so a naive ceiling check would pass one), bounded nesting, full contract validation, and writer-owned fields (`provenance`, `decision_id`) refused loudly rather than stripped. Refusal messages never echo payload content, so a hostile worker cannot write chosen text into operator logs. **Residual (disclosed):** authenticating *which* worker is calling is the transport's job — a Unix socket with filesystem permissions, or loopback plus the existing API token — and Chronos deliberately does not invent a second, weaker scheme. Chronos also ships no process supervisor that *starts* the worker; running it separately is an operational act. |
| R-37 | Model self-sizing widens the size envelope (`model_discretion`) | C | OPEN (ACCEPTED, owner-directed) | ADR-0017/D-17. Under an owner-granted `model_discretion` flag, an UNSET capital ceiling no longer clamps to zero — sizing is bounded by affordability (cash/buying-power net of the floors) instead. This is a deliberate inversion of deny-by-default, **scoped to the ceilings and only where the owner wrote the flag**. It does not touch: the `min_*` cash/buying-power **floors** (validated before the waiver returns — discretion over size is not discretion over the reserve), any ceiling the owner *did* set (still binds, still refuses on absent evidence), the drawdown/session breakers, the kill switch, or expiry. An owner wanting a hard per-order cap under discretion sets `max_order_notional_usd` — it still binds. Residual is real and accepted by explicit owner directive; it broadens R-29 rather than creating a new class. Tests: `tests/safety/test_supervisor_gateway.py` (discretion suite). |
| R-38 | Persistent auto-activating mandate re-arms on restart | H | MITIGATED (M7.5/ADR-0017, 2026-07-25) | A valid, account-matching `AUTONOMY_MANDATE_FILE` auto-activates on every boot (supersedes ADR-0016 §4's "env var alone may not activate live"), so bringing the process up is enough to trade. Bounded by: **revocation survives restart** — `ensure_activation` refuses to re-arm a revoked mandate and alerts instead, so the off switch is durable and a restart is not permission to undo it; **invalid/unreadable/wrong-account file → inert + CRITICAL alert** (the process that can still close positions is never taken down by a broken grant); **expiry still expires**; the activation is **digest-stamped** so the audit trail records which text granted authority and an edit writes a distinguishable row; and **no file → no runtime**. `MAX_LIVE_MANDATE_DURATION` raised 30d→365d (a judgment, longer not infinite). **Residual (disclosed):** "trading off" now requires a durable revoke or file removal, not merely a restart — the deliberate inversion the owner chose. Tests: `tests/safety/test_autonomy_wiring.py`. |
| R-42 | The terminal makes historical requests on the connection that submits orders | M | MITIGATED (M8c/ADR-0019, 2026-07-26) | Before M8c the backend never asked IBKR for historical data; the chart panel means it does. IBKR paces `reqHistoricalData` far harder than quotes, and this process holds **one** connection shared with the order pipeline and the autonomy tick, so an unbounded chart would contend with submissions against the gateway's most rate-limited surface. Bounded by: a cache keyed `(symbol, interval)` that remembers its coverage depth, so completed-and-immutable bars are served without re-asking and a narrow window is sliced from a wider cached series rather than refetched; a **two-minute** panel cadence rather than the terminal's ordinary five seconds; `PacingController` (shared with the histdata plane, ADR-0011 §5); a bounded LRU so the cache cannot leak in the broker-holding process; and a `MAX_LOOKBACK_DAYS` ceiling. **Pacing degrades, never blocks** — a paced-out request serves the cache labelled `stale` or refuses, because sleeping in the request handler would convert a rate limit into latency for order submission. Budget is recorded **before** the call, not after a success: a failure that consumed nothing would let a bad symbol retry every poll unthrottled (a real defect in the first implementation, caught by its own test). **Residual (disclosed):** the budget is this process's own; the histdata backfill process paces separately under a different client id, and whether IBKR's real limits are shared across client ids cannot be determined without a live gateway — ADR-0011 §5's original residual, now with two self-pacing callers. The IBKR path itself is wired and fake-tested but gateway-unverified, exactly as the order surface was at M7. Tests: `tests/safety/test_terminal_bars.py`. |
| R-43 | The live kill switch and session disarm are reachable from the browser for the first time | M | MITIGATED (2026-08-02) | Until now the terminal's only emergency stop was `curl` with the raw API token: the M8b session cookie is scoped `path=/terminal`, so the shipped client structurally could not call `POST /live/kill` (`test_the_session_does_not_authenticate_the_order_plane` pins that, and still passes). `POST /terminal/live/kill` and `POST /terminal/live/disarm` close that gap. Like R-39 this **is** an authorization-surface change and is recorded as one rather than slipped in. What bounds it: **only the authority-REMOVING half of ADR-0018 §4's grant is exposed** — arming and kill-disengage *grant* authority and are deliberately absent, pinned by `test_the_terminal_offers_no_route_that_grants_live_authority`, so re-enabling trading remains a token-and-lease act; both routes **fail in the safe direction by construction**, exactly as revocation does; a reason is required on the kill route (422 otherwise) and is never echoed; a typed confirmation phrase is required client-side; and the session's boundary is unchanged — the cookie still 401s on `/orders/*`, `/account/*`, and `/live/*`. **Deliberately NOT writer-gated**, mirroring `POST /live/kill` and `/live/disarm`: a backend that lost its lease is the one whose operator most needs the stop, and the client's read-only block is bypassed for these two for the same reason (a disabled stop button would be the UI inventing a gate the server does not have). The route module still imports nothing from `chronos.orders`/`broker`/`execution` — the two response models are declared locally — so ADR-0018 §4's import-graph invariant holds. **Residual (disclosed):** this stops the `chronos.orders` live plane only; the deterministic platform's halt is a separate control (`python -m chronos.cli halt`), and an active autonomy mandate still needs a separate revoke. `docs/INCIDENT_RESPONSE.md` now sequences all three. Tests: `tests/integration/test_terminal_api.py` (6). |
| R-44 | Five-Tool trials could run without being counted, understating the multiple-testing N | M | MITIGATED (2026-08-09, ADR-0013 §11 update) | **The residual ADR-0013 disclosed about itself: "completeness of the trial count ... auto-registration of the runner is a follow-on".** A ledger that counts only the runs someone remembered to register reports N=3 while the world did 40, and the deflated Sharpe is computed against that number. `FiveToolTrialBroker` now writes an `experiment_run` record into the canonical hash-chained registry **before** the durable local start and therefore before any read, verifying chain **and** head anchor first and failing closed: an unwired, unprovisioned, unreadable, chain-broken, or truncated registry refuses the trial with a distinct reason and the reader is never called. An attempt that dies after opening data is still counted, and a campaign whose attempts were not registered cannot be sealed. Ordering is deliberately conservative — a local start that fails after the canonical record lands over-counts N, which raises the bar; under-counting is the failure being prevented. **Residuals (disclosed):** (a) this covers the Five-Tool path only — `walkforward.py` still registers *last* by design (it is handed an already-read series) and any other caller counts only if it registers itself; (b) ~~the arbitrary reader callback still cannot prove what data it touched — that waits on the certified reader~~ **Corrected 2026-08-09 (same day, certified-reader capability): residual (b) is narrowed, not closed.** `CertifiedDatasetReader` proves a read — it is digest-locked to a certification manifest binding every file to a SHA-256 and the dataset to a bytes-derived overall digest, it attests those digests before registration and its receipt of what it actually opened is re-checked against that attestation and against the returned bytes afterwards, so `data_hashes.certified_reader` is `True` only for a proven read. **What remains:** only the exact reader type earns the claim, so an arbitrary callback, a subclass, a wrapper, or mixed access still records `False` — a run may still be *registered* without proven provenance, it simply can no longer *claim* provenance it lacks; and the certification manifest authorizes a partition **label**, it does not carve holdout bars out of a dataset that contains them, so a certified dataset must not contain holdout bytes (an owner certification decision, not a reader guarantee); **Corrected 2026-08-09 (same day, replay-artifact capability): residual (b) is narrowed again in a second direction — reproducibility.** A counted, provenance-proven trial could still be impossible to *re-execute*: the ledger recorded `evidence_artifact_sha256` and threw the bytes away, so a completed trial could be sealed and cited while nobody who did not already hold the artifact could re-derive it. Any attempt that opens data now persists a content-addressed replay artifact (`chronos.research.five_tool.replay`) holding campaign identity, engine identity **including the semantic configuration itself**, the input locks and the digest of the bytes read, the certified attestation and receipt digests, the durable start, and the outcome — for a completed attempt the evidence bytes themselves. The artifact is written **before** the terminal record and named by it, so a completed trial that cannot be replayed cannot exist; an unwired or unprovisioned store refuses the attempt before it is registered or read; sealing re-verifies every scored row against the store in both directions (present, hashing to its own content address, and describing the attempt the ledger says it describes); and `replay_trial` refuses on any byte divergence, naming which axis moved (inputs, configuration, certification, outcome, or outputs) with a digest backstop so a field added to the schema and forgotten in the comparison cannot pass. **What remains:** the artifact does not copy the dataset, so replay depends on the supplied reader still producing those bytes; a replay registers **no** trial (a reproducibility probe, the `research/repro.py` stance) so it neither inflates nor validates N; and reproducing a number proves determinism only, never that the number is correct, profitable, or inside limits nobody has frozen. (c) the registry ships **empty**, **no dataset is certified** — no `CERTIFICATION.json` exists under `research/` — and **no replay artifact exists** under `research/` either, so all three are capabilities, not evidence, and the campaign manifest stays blocked on owner evidence alone (~~certified reader,~~ ~~replay artifacts,~~ removed 2026-08-09). Each refusal conjunct was verified by reverting it alone and confirming a distinct named failure. Tests: `tests/safety/test_five_tool_registry_exercised.py` (~~22~~ **20 as of 2026-08-09** — the count fell as its capability-list parametrization shrank with each capability that landed, not because a test was deleted), `tests/safety/test_five_tool_certified_reader_exercised.py` (39), `tests/safety/test_five_tool_replay_exercised.py` (43), and the companion `tests/safety/test_five_tool_provenance_audit_exercised.py` (42) for the lookahead-provenance identifiers those runs record. |
| R-39 | Mandate revocation is reachable over HTTP for the first time | H | MITIGATED (M8a/ADR-0018, 2026-07-26) | Revocation existed only as `chronos.supervisor.durable.revoke()` with no route and no CLI; the operator terminal needs an off switch, so `POST /terminal/mandate/revoke` now exposes it. This **is** an authorization-surface change and is recorded as one rather than slipped in. Bounded by: the router-level API token; `require_writer`, so a read-only backend refuses; a **required non-empty reason** (422 otherwise — an unexplained revocation is not auditable); an optional `mandate_id` binding the recorded act to the grant the owner was actually shown, refused with 409 when it names a different grant, which closes the ADR-0017 window where an auto-activated newer mandate could be revoked under the name of the older one the panel displayed; a typed confirmation phrase client-side; and `durable.revoke`'s existing behavior of marking the activation in place and appending `mandate_revoked` with the reason to the per-account authority hash chain. Fails in the safe direction by construction — revocation only ever *removes* authority. `revoked: false` (nothing was in force) is a 200 answer, not an error, and the terminal now reports it as such rather than as success. Tests: `tests/integration/test_terminal_api.py`. |
| R-40 | The terminal renders untrusted model narrative to the owner | M | MITIGATED (M8a/ADR-0018, 2026-07-26) — extends R-30 | Thesis, rationale, refusal detail, and alert summaries derive from external text an injection can influence (R-30), and M8a gives them an owner-facing renderer for the first time. Three layers, deliberately not one: **(1)** every server string reaches the DOM through `textContent` — nothing in the client assigns `innerHTML`, `outerHTML`, `insertAdjacentHTML`, or `document.write`, and no `eval`/`new Function` exists; **(2)** that property is now **structural, not a comment** — `tests/safety/test_terminal_client_has_no_html_sinks.py` reads the shipped client, strips comments so the file's own honest description of what it avoids cannot satisfy its own test, and fails on any sink, inline `on*` handler, `javascript:` URL, or external origin; **(3)** a Content-Security-Policy (`default-src 'none'`, `script-src 'self'`, `frame-ancestors 'none'`, plus `nosniff`) over `/terminal/*`, so the *next* `innerHTML` is inert. The page loads nothing external, so the CSP costs no functionality. **Residual (disclosed):** the terminal is a place injected text can be *seen* — that is intended and is the point of showing the owner what the model believes; it must never become a place injected text can *act*, which is what layers 2 and 3 exist to keep true after this milestone's authors have moved on. |
| R-41 | The terminal shell is served unauthenticated and its client holds no credential | M | CLOSED (M8b, 2026-07-26) | **Owner chose the session-cookie route.** `POST /terminal/session` exchanges the local API token for an httpOnly cookie, and every `/terminal/*` route now accepts either that cookie or the `X-Chronos-Token` header — so scripts and `curl` are untouched while a browser, which cannot header a document load, can authenticate itself. **The load-bearing property is the cookie's `path=/terminal` scope:** the browser never attaches it to `/orders/*`, so the M8a injection review's named worst case — script in the terminal page riding an ambient credential into order submission from the process holding the broker connection — is structurally closed rather than merely unlikely. Verified at the *server*, not by trusting the browser to honour the path: `test_the_session_does_not_authenticate_the_order_plane` asks `/orders/*`, `/account/*` and `/live/*` with the session and no header, and all still 401. Also bounded: `SameSite=strict`; sessions held **in memory only**, so a restart signs every terminal out (a credential outliving its process is exactly what this project refuses elsewhere); a 12-hour TTL checked on every use, not only on sweep; ids stored as SHA-256 digests so a memory disclosure leaks nothing presentable; a 32-session ceiling that refuses the *new* login rather than evicting an old one (evicting would let anyone holding the token sign the operator out); and the token is never sent to the browser, only compared against, so the page cannot learn or forward it. **A session is not authority** — `require_writer` still gates every mutation independently, proven by `test_the_session_grants_no_writer_authority` (a demoted backend accepts the login and still 409s the revoke). Tests: `tests/integration/test_terminal_api.py` (9 session tests). |
| R-41-orig | (historical detail of R-41) | M | CLOSED | A browser cannot put a header on a document load, so `/terminal/app` is served without the API token — from loopback, from this process, from files shipped in the package. Every `/terminal/*` **data** route still requires `X-Chronos-Token`. The honest consequence: **the bundled client sends no token, so a browser that loads the shell gets 401 on every panel.** The terminal is therefore not yet usable end to end. This was left open deliberately rather than solved in passing — the options (a browser session cookie, a pasted token in `sessionStorage`, exempting loopback GETs) differ in how much authority they hand to JavaScript, and that decision does not belong in the same commit that first exposes mandate revocation (R-39). Same-origin serving means there is no CORS, no preflight, and no credential handed to script from another origin, so the posture is unchanged by the shell existing; what remains is choosing how the operator's own token reaches their own browser. |
| R-36 | No scheduled runner drives the autonomy cycle | M | MITIGATED (M7, 2026-07-25); wiring residual CLOSED (M7.5/ADR-0017, 2026-07-25) | **The tick exists and is now wired.** `supervisor.runtime.AutonomyRuntime` drains the durable proposal queue, delivers pending alerts, and observes its own health on a time-driven tick. **Events are hints, not triggers:** `note_event()` can pull the next tick forward to the configured floor and no further, and any number of events coalesce into one early wake — so an event storm cannot become an order storm, and cycles-per-hour is a property of configuration rather than of the day's volatility. A zero floor is refused at construction. Supervision: a failing tick alerts and keeps cadence rather than dying silently; consecutive failures past the configured bound stop the runtime with a CRITICAL alert, and restart is an operator act. The proposal route enqueues into a bounded durable queue (newest refused at the cap) and the runtime judges on its own clock. **Wiring closed:** `chronos.api.autonomy_wiring.build_autonomy_runtime` constructs the runtime from settings with a real `FactGatherer` (broker account summary + probe quote), a real per-decision instrument gatherer, and an `order_plane_handoff` that walks the full propose→preview→confirm→submit pipeline; the backend lifespan constructs it and drives `autonomy_tick_task`. No mandate file → no runtime (inert by design). Tests: `tests/safety/test_autonomy_wiring.py`. |
| R-45 | A second, internet-reachable proposal source exists (TradingView bridge) | H | MITIGATED (2026-08-12, ADR-0026/D-22) — ships inert | Until now the only way to reach `POST /autonomy/proposals` was to already be a local process holding the API token. `chronos.bridge` adds a caller that can be driven from the public internet, and that is an authorization-surface change recorded as one rather than slipped in. What bounds it: the bridge **holds no Chronos capability** — an AST walk plus a subprocess import probe assert it reaches no order, broker, execution, risk, persistence, supervisor, registry, or database module, **and no `chronos.autonomy` module either**, so it emits a `dict` and the existing hostile-input ingress decides whether that dict is a proposal (there stays exactly one place where "what a valid decision is" is decided, and a second one was deliberately not created); it adds **no authority** — every downstream gate is untouched, so a TradingView-sourced proposal walks the same fifteen admission checks, the same sizing against mandate ceilings and floors, the same capability-matrix compiler, and the same propose→preview→confirm→submit handoff, and the mandate still bounds what it can become; it **ships inert** — `CHRONOS_TV_BRIDGE_FORWARD` defaults false so the shipped posture translates and logs but sends nothing, a secret of ≥32 chars is mandatory (TradingView cannot set request headers, so the body is the only channel), both the symbol and kind allowlists are required and empty means *nothing* rather than everything, and a non-loopback `CHRONOS_TV_BRIDGE_INGRESS_URL` refuses to boot because the bridge POSTs carrying the backend's own API token; and it adds its own refusals before forwarding — rate limit, bounded size, constant-time secret check **before any other field is judged** (so a prober learns only that it is unauthorized), freshness against the alert's own `{{timenow}}`, and replay refusal on `alert_id`. **No refusal message at any layer quotes a value the sender supplied**, not even one from Chronos's own closed vocabulary — the response is written into TradingView's log, outside the trust boundary — and the specifics go to the bridge's log instead. The duplication the non-importing choice forces is guarded structurally: `test_tradingview_bridge_isolation.py` pins every restated enum, frozenset, symbol alphabet, reference pattern, and the auth header **equal** to the real thing, and `test_tradingview_bridge_exercised.py` pushes the bridge's own output through the real `ingress.parse_proposal` per decision kind, so "the ingress accepts what the bridge emits" is proven rather than assumed. Equities only (options refuse at the wiring's instrument seam anyway); the four ADR-0021 inert fields are deliberately never populated. **Residuals (disclosed):** (a) **provenance still cannot distinguish the sources** — every proposal is stamped with the static `INGRESS_IDENTITY`, so a TradingView-sourced decision's provenance is byte-identical to a model worker's; the bridge compensates only where it can, writing an evidence citation with `kind="tradingview_alert"` whose digest is over the exact alert text with the secret removed, so the audit chain records which alert produced which decision and the owner can recompute it — ~~plan §6 finding 6 and ADR-0023 stay **open** and this row makes them more acute, not less~~ *(corrected 2026-08-12: ADR-0023 Option A accepted and built — D-24/R-48. With `AUTONOMY_PROPOSERS_FILE` configured, the bridge presents its own registered credential via `CHRONOS_TV_BRIDGE_PROPOSER_TOKEN` and its proposals are stamped with its registration's identity, distinguishable from every other proposer's)*; (b) ~~the credential is the same local API token every mutating route accepts, not a proposal-only one~~ *(corrected 2026-08-12: under a configured registry the proposal route requires a proposal-only credential and refuses the general token; unset, the pre-registry posture remains, by design)*; (c) anyone holding the URL and the secret can propose, which is the threat model of a webhook rather than a defect introduced here; (d) publishing the listener beyond loopback needs a tunnel or reverse proxy the owner runs, terminates TLS for, and owns the risk of — nothing here manages that and ADR-0026 does not authorize it; (e) the rate limit and replay cache are per-process and in memory, so a restart forgets both — the durable bounds (economic-content `decision_id`, `MAX_RESUBMISSIONS`) are unaffected and remain authoritative; (f) as everywhere else on this path, no real gateway has ever been connected, so everything downstream is fixture-verified only. **An alert is not evidence:** nothing here creates a promotion rung or a research trial. Tests: `tests/safety/test_tradingview_bridge_isolation.py` (11), `tests/safety/test_tradingview_bridge_exercised.py` (29), `tests/unit/test_tradingview_bridge.py` (26). |
| R-47 | A generative model now actually originates trading decisions (the model worker) | H | MITIGATED (2026-08-12, ADR-0027/D-23) — ships inert | Until now every control on the proposal path was exercised only by tests and, since R-45, by TradingView alerts: no model had ever proposed a trade. `worker/` fills ADR-0016 §3's deliberately empty seat — an external worker that calls IN — and the risk it adds is bounded the way the ADRs always planned. What bounds it: **total import isolation** — the worker imports NOTHING from `chronos` (stronger than the bridge; AST walk + subprocess probe), and nothing in `src/chronos` imports the worker, so the process holding the LLM API key and reading model output has no broker handle, session, lease, kill switch, or submission path in its address space; **no provider SDK in-repo** — the worker speaks to the Messages API over raw httpx, and the standing manual grep for `anthropic|openai|litellm|langchain` in the dependency files is now a structural test (both this and the import guard were verified by injecting each hazard and watching the test fail); **the forced strict tool schema is the model's entire output universe** — `additionalProperties: false` means provenance, decision ids, accounts, prices, routing and transmit are unrepresentable rather than filtered, and prose is never parsed into a trade because there is no prose channel; **provenance is stamped, never authored** — the single evidence citation's digest is the SHA-256 of the exact canonical bytes rendered into the prompt, so the model cannot claim to have seen evidence it was not shown; **fail-closed posture** — `CHRONOS_WORKER_FORWARD` defaults false (gather, think, log, send nothing), required non-empty watchlist and kind allowlists, loopback-only backend URL because the worker carries the backend's API token, evidence gathered or the cycle refuses to think, and a model refusal/truncation/prose answer yields no proposal; and **no added authority** — a forwarded proposal walks the identical ingress and gate stack as everything else, the mandate remains the only grant, and a worker running against a mandate-less backend produces judged refusals. **Residuals (disclosed):** (a) ~~`INGRESS_IDENTITY` now covers three indistinguishable sources (worker, TradingView bridge, any local token-holder) — the evidence-citation kind (`worker_evidence_snapshot`) is the only distinguishing mark, and plan §6 finding 6 / ADR-0023 moves from open to overdue~~ *(corrected 2026-08-12: ADR-0023 Option A accepted and built — D-24/R-48. A registered worker sets `CHRONOS_WORKER_PROPOSER_TOKEN` and its proposals are stamped with its registration's identity, journaled with its proposer_id; the static identity persists only under the registry-unset posture)*; (b) `worker/policy.md` is unpinned strategy authority — bounded by the gate stack but unattributable across edits; *(sharpened 2026-08-12: the registration record now carries a per-author `prompt_version` the owner can bump on each policy edit and the mandate can pin, but it is an owner-typed label — nothing digests the policy file's content into it, so content-hash binding stays future work)*; **~~content-hash binding stays future work~~ narrowed 2026-08-14 (A4; R-52): `proposer mint --policy-file` derives `prompt_version` as `sha256(policy bytes)[:16]`, so an edited policy no longer matches the mandate's pin and admission refuses `VERSION_PIN_MISMATCH` until the owner re-pins. What remains open is the half no CLI can close: nothing proves which policy the worker *ran*;** (c) R-30 prompt injection stays bounded-not-solved, with backend-fetched text fields as the worker's injection surface and the framing prompt plus the contract's control-character refusals as the mitigation; (d) the worker trusts loopback — nothing authenticates the backend to it; (e) ~~cost is logged, not capped — cadence is the only throttle~~ *(narrowed 2026-08-21, A5: `CHRONOS_WORKER_MAX_DAILY_TOKENS` caps model tokens (input + output) per UTC day in the worker's own memory — at the ceiling cycles log `COST_CEILING` and skip thinking, evidence reads included, until the day rolls; a priced response reporting no usable usage is charged the full `max_tokens` rather than nothing, and an unparsable or non-positive ceiling refuses to start. What remains open, disclosed at startup and in `docs/model_worker.md`: unset stays uncapped, and the counter is in-process — a restart forgets the day's spend, so the ceiling bounds a running worker, not a supervisor that restarts it)*; (f) as everywhere on this path, no real gateway has ever been connected, so a SHADOW worker proves the decision loop against fixtures, not the broker. **A worker decision is not evidence**: no registry write, no rung, no statistical claim. Tests: `tests/safety/test_model_worker_isolation.py` (12), `tests/safety/test_model_worker_exercised.py` (22), `tests/unit/test_model_worker.py` (25). |
| R-48 | Proposal authorship became an authorization surface (proposer registry + proposal-only credential) | M | MITIGATED (2026-08-12, ADR-0023/D-24) — fail-closed in every posture | ADR-0023 Option A added the thing R-39 teaches to treat with care: a new credential store. What bounds it: **the registry is a grant, shaped like the mandate** — owner-authored JSON named by `AUTONOMY_PROPOSERS_FILE`, validated on boot, digest-logged, written by no CLI and no route (`chronos.cli proposer mint|check` are stdout-only, pinned by a writes-nothing test); **the file holds only SHA-256 hashes**, so reading it yields no credential, and verification visits every entry in constant time; **everything expires** — a registration without a future expiry never verifies, and identity is re-resolved at drain time against the drain's clock, so an expiry crossed between enqueue and drain refuses at STAMP rather than stamping a dead registration; **the credential is proposal-only in both directions**, exercised by enumerating every mutating route in the app's own route table and presenting the credential every way a confused process could (proposer header, token header, terminal session body) — all 401; **the general token is refused on proposals under registry-on**, so the split cannot silently degrade to either-or; **no failure falls back** — unset means the pre-registry posture verbatim, configured-but-broken refuses every proposal at the route (503) and the drain (STAMP) with a CRITICAL owner alert, and an unresolvable row (unknown id, pre-registry NULL met by a registry-on runtime) refuses rather than borrowing the static identity (each hazard injected and watched fail); **provenance and the proposal journal now record the verified proposer_id**, and the mandate's pins bind to that authorship at admission check 8, with `mandate check` made registry-aware so a mandate authored against the wrong reference is BLOCKING at authoring time instead of silent VERSION_PIN_MISMATCH refusals at trade time. Building the proofs surfaced a live defect this row exists to prevent elsewhere: the proposal route's account scoping read an attribute that never existed, so every real proposal POST since M7 had refused 503 `BACKEND_UNSCOPED` and the alert pull endpoint always returned `[]` — the ingress the last two rows documented was structurally dead. Fixed by deriving the fingerprint the way the terminal does; the 202-that-actually-enqueues and the alert-that-actually-lists are pinned as revert-the-fix guards. **Residuals (disclosed):** (a) authorship pins by version tuple, not name — two registrations with identical version fields are indistinguishable to admission (ids and hashes must be unique; version tuples need not be), and `mandate check` listing every match is the visibility, not a refusal; (b) evidence binding stays uniform across proposers — registrations carry no evidence-bundle fields, every identity stamps the placeholder bundle id with an honestly-absent digest (`None`, no longer sixty-four zeros), and admission refuses a digest disagreement in every direction including claimed-when-absent; (c) ~~**the registry is a boot-time snapshot on both planes** — the route and the drain each load the file once at startup, so disabling or deleting a registration is honored at the next restart, not mid-session (only expiry, which the snapshot carries with it, transitions live); a leaked credential mid-session is answered by the kill switch, mandate revocation, or a restart — all live — and its proposals meanwhile still face the entire gate stack and grant no authority;~~ **narrowed 2026-08-14 (A3; R-51): a leaked credential is now answered directly and live by `chronos.cli proposer revoke`, a durable hash-chained act both planes consult per use — no restart. What remains of this residual is the rest of the snapshot: *enabling*, re-registering, or editing a registration's identity fields still lands at the next restart, which is the correct latency for a grant and the wrong one only for revocation, which is why revocation is the half that moved;** a file edited between the two loads can diverge until restart, and both sides fail closed; (d) the registry secures authorship, not transport — loopback binding and file permissions remain the perimeter, and nothing authenticates the backend to the worker; (e) header constants are restated in the worker and bridge, pinned equal to `chronos.api.auth`'s by isolation tests (a silent rename would otherwise 401 every registry-on forward with all tests green); (f) as everywhere on this path, no real gateway has ever been connected — every proof is fixture-verified. Tests: `tests/safety/test_proposer_credentials_exercised.py` (15, incl. per-route enumeration, drain-time revocation, journal-matches-credential, digest tri-state, CLI writes-nothing), `tests/safety/test_mandate_check.py` (+5 registry-aware pins), `tests/integration/test_migrations.py` (+1 true-pre-0007 upgrade), header pins in both isolation suites, forward-header coverage in both unit suites. **Amended 2026-08-14 (ADR-0028/D-25):** the proposal route is no longer the only route this credential opens. `POST /autonomy/evidence` (bundle issuance) is a second, and the per-route enumeration test carries it as a **named, tested exception** rather than absorbing it — `_PROPOSER_CREDENTIAL_ROUTES` in `tests/safety/test_proposer_credentials_exercised.py` lists exactly the two permitted routes and fails if the app's route table and that list disagree in either direction, so a third route cannot arrive silently. Each exception has its own positive control (proposals: `test_registry_on_credential_decides_and_the_row_records_who`; issuance: `test_the_issuance_route_is_the_credentials_one_named_exception`), because a named exemption nobody checks is just an exemption. ADR-0028's own recommendation is recorded with it: this should be the last route that credential opens without a further ADR. |
| R-49 | The autonomy journal reported refusals as completed orders, and the activity ceiling was spent on orders that never existed | H | MITIGATED (2026-08-13, plan §6 finding 5; A1) — the misreport is closed in both directions; the disclosed residuals are about what no external observer can know | The supervisor asked the handoff one question — *did it raise?* — and read every other answer as success (`loop.py`, pre-A1 `:428-476`). But the submission boundary **returns** its refusals: `SubmissionOutcome(submitted=False, ...)` for `READ_ONLY_LEASE`, `MODE_FORBIDS`, `LIVE_GATE_BLOCKED` (which is how a kill switch tripping *inside* the CAS-to-transmit window comes back), `RECONCILIATION_NOT_READY`, `BROKER_REFUSED_BEFORE_SEND`, and the ambiguous `BROKER_SUBMIT_FAILED`. Each journaled `stage=COMPLETE` and incremented `orders_submitted`. **Wrong in both directions at once:** permissive about budget — a read-only or unarmed backend could spend a whole session's opening-order allowance on orders the venue never heard about, so the ceiling rationed nothing — and quietly reassuring about risk: an ambiguous send, whose resolution is an owner act (plan §11 "manual broker resolution of ... ambiguous sends"), recorded a clean COMPLETE and raised no alert. Same defect class as R-24..R-27: not a control that failed, a control whose *evidence* was never read. **The fix:** `chronos.supervisor.handoff` owns four dispositions (SUBMITTED, REFUSED_NOT_SENT, SENT_AMBIGUOUS, REJECTED_AFTER_SEND), each journaling its own `CycleStage` and refusal code, and states the counting rule once — **an activity attempt is consumed exactly when the supervisor cannot prove nothing reached the wire.** A refusal before the wire attempted nothing and spends no budget; an unknown wire state counts, because over-counting narrows the system's own authority while under-counting hands back budget the venue may already hold. SENT_AMBIGUOUS additionally raises a CRITICAL owner alert (REJECTED_AFTER_SEND a WARNING). The two new stages and every new refusal code are **additive** — no existing refusal weakens, `ORDER_PLANE_REFUSED` still names an exception out of the callable, and `COMPLETE` now means a confirmed send and nothing weaker. The supervisor acquired a typed result **without** acquiring an order-plane type: translation happens at the app-plane seam (`classify_submission_outcome`, `api/autonomy_wiring.py`), behind an explicit table classifying every `SubmissionRefusalCode` as provably-not-sent or possibly-sent, and `BROKER_SUBMIT_FAILED`'s three real shapes are separated by the evidence the outcome carries rather than by parsing its prose (no submission object, and an acknowledged-but-unpersisted *live* order, are both ambiguous; an acknowledged non-active lifecycle is rejected-after-send — reading "we lost the record of a working order" as a clean rejection was the most dangerous interpretation available). An answer the supervisor cannot classify is ambiguous-and-alerted, never success — including `None`, which a duck-typed `getattr(result, "submitted", None)` would have read as a refusal and silently un-counted. One pre-existing test was pinning the defect as intended behavior (`test_counting_happens_at_handoff` asserted the counter advanced for a value whose only readable content said the order was *not* submitted — the R-27 pattern), and is rewritten. Tests: `tests/safety/test_typed_handoff_outcomes_exercised.py` (42; each disposition driven through a real session, the real hash-chained journal and the real durable counters, asserting stage, refusal code, counter and alert; every refusal code pinned as classified so a new order-plane refusal cannot default into silence), each of **six** independent conjuncts verified by reverting it alone and confirming a distinct named failure. **Residuals (disclosed):** (a) **an exception out of a handoff callable is still recorded as not-sent and counts nothing** — the production wiring now catches anything the submit call itself raises and reports it as possibly-sent, but a *different* caller whose handoff transmits and then raises would be journaled as a refusal; the isolation that keeps the supervisor plane-clean is exactly what prevents it from knowing better; (b) "provably not sent" is a **classification of the order plane's own codes**, not an independent observation — it is verified against the boundary's source and pinned by a guard-the-guard test, and a boundary change that returned an existing code from a new post-transmit site would be misclassified until someone re-read it (the pinning test makes an unlisted *new* code fail closed, not a moved *old* one); (c) `SENT_AMBIGUOUS` counts an attempt that may not exist, so a flapping broker can exhaust an activity ceiling and stand the system down — deliberate, and the direction a fail-closed system must choose; (d) the alert is still **pull-only** (R-32): an owner who is not looking is not told, which is why unattended LIVE_AUTONOMOUS remains gated; (e) nothing here is gateway-verified — every shape is a fixture, and `BROKER_SUBMIT_FAILED`'s real-world distribution against a live IBKR gateway has never been observed (§7 campaign, MITIGATED ≠ CLOSED); (f) the four typed outcomes plan §6 additionally names — partial fill, full fill, cancellation, late commission — are **not** part of this work: they are lifecycle events after submission, and the order plane's tracker, not the cycle's handoff, is where they belong. |
| R-50 | Per-job evidence binding: a new authorization surface, and a protocol that invites more belief than it earns | M | MITIGATED (2026-08-14, ADR-0028/D-25) | **What it closes.** Admission check 9 compared a constant against itself: the decision side and the expectation side were two reads of `INGRESS_IDENTITY`, so the check could not refuse in any posture, for any proposer — the R-24..R-27 inert-control shape one level up, caught before it cost anything. Option C gives it two independent origins: the drain resolves the cited bundle against a durable hash-chained record (`autonomy_evidence_bundles`, migration 0008, schema v9) at **its own clock** and stamps provenance from that record, and check 9 additionally compares the payload's own `EvidenceCitation` — which the proposer authored and the backend never wrote. **Residuals, disclosed:** (a) **equality catches accident, not malice** — a proposer that fetches a bundle, reasons on entirely different text and cites the issued digest is indistinguishable from an honest one, because the backend cannot observe a prompt in another process; what is caught is rendering drift (truncation, reordering, key-order change, partial fetch), which is the class that produced R-24..R-27; (b) **attested is not witnessed** — for the TradingView bridge, and any future proposer whose evidence originates outside Chronos, the record binds a claim to a credential and a time. Non-repudiation, not verification. The recommended rule that an attested bundle may back a proposal and never a promotion rung is ADR-0024's to adopt or reject, and is enforced here only as the kind-substitution refusal; (c) **a bundle binds which facts were served, never that they were true** — every fact comes from a fixture-verified adapter path, no real IBKR gateway has ever been connected, so a bundle proves the backend served these numbers, not that the broker held these positions. Nothing here leaves MITIGATED; (d) **a proposer may still choose *when* within the TTL to fetch**, selecting a favorable moment inside stale-but-unexpired evidence (bounded only by the TTL, and separately by admission's market-data freshness check, which reads the supervisor's quote and not the bundle), and may back several proposals with one bundle inside the window — deliberate, since single-use would collide with `MAX_RESUBMISSIONS`'s retry budget and punish honest retries; (e) **new authorization surface** — issuance is a write reachable by a proposal-only credential, bounded by a per-proposer live-bundle cap (64, refusing rather than evicting an in-flight bundle), the writer lease, a symbol bound on composition work, and R-48's named-exception enumeration; (f) **operational coupling** — requiring digest equality couples the backend's serialization to the proposer's rendering: a change to either refuses every forward until both move. Fail-closed in direction, version-pinned via `bundle_version`, and the reason the worker renders the served document *verbatim* rather than re-serializing it; (g) **the chain is tamper-evident, not tamper-proof** — inherited from `persistence/hash_chain.py`, not narrowed here; retention prunes the expired lookup row and never the issuance record; (h) **the registry stays a boot-time snapshot** (R-48(c)) — issuance inherits that posture and does not improve it; live proposer revocation remains item A3; (i) **none of this makes a worker's decision evidence** — no registry write, no rung, no statistical claim. A per-job evidence record is exactly the artifact someone will later be tempted to read as research evidence, and it is not one. **Found while building this:** a second live dead-route defect — `chronos.api.bars.provider_for` cached by assigning a new attribute to the `slots=True` `BackendState`, so `GET /terminal/bars` answered 500 for every symbol, on every backend, since the route existed; no test had ever called it. Fixed (declared slot + graceful degradation) with the route driven to a 200 and the cache identity asserted. Tests: `tests/safety/test_evidence_bundles_exercised.py` (25), each conjunct verified by reverting it alone and watching a named test fail (18/18); `tests/integration/test_migrations.py` (+1 true-pre-0008 upgrade); `tests/safety/test_proposer_credentials_exercised.py` (named-exception enumeration). |
| R-51 | Revoking a leaked proposer credential required restarting the broker-holding process | M | MITIGATED (2026-08-14, A3) — live at both enforcement points, permanent per credential | R-48(c)'s residual, closed for the half that matters. The proposer registry is an owner-authored file read once at boot by both the route and the drain, so the only way to stop a leaked credential was to edit the file and restart — a restart of the process that holds the broker connection, during the incident, which is when an operator least wants to bounce it. Meanwhile the leaked credential could keep proposing; those proposals faced the whole gate stack and granted nothing, but they were attributed to a proposer the owner believed was stopped, and the journal would have said so. **The act:** `chronos.cli proposer revoke --file <registry> --proposer-id X --reason "..."` writes one row in `autonomy_proposer_revocations` (migration 0009, schema v10) and one hash-chained record in the un-scoped `autonomy.proposers` stream. Both planes consult it per use — `require_proposer` at the route (401, sharing the existing message so a 401 body still discloses no registry state) and the drain-time identity resolver at STAMP (`PROPOSER_REVOKED`, a new refusal code so the journal can distinguish a credential the owner killed from a registration that merely aged out). Nothing is cached, because a cache is what made the file a snapshot. **Keyed by credential hash, not by proposer id**, which is the design decision that matters operationally: revoking burns the secret that leaked, not the name, so the recovery path is "mint a new credential for the same proposer and restart" rather than "invent claude-worker-2 forever". **Not account-scoped:** a credential is global to the registry document, and an account-scoped revocation would leave a state in which a revoked credential still proposes somewhere. **Authority-removing only:** there is no un-revoke, `revoke` never writes the registry file (the grant stays owner-authored — which is exactly why revocation can be live), and `mint`/`check` remain stdout-only. `proposer check` reports REVOKED, and reports **UNVERIFIED** — never CURRENT — when it could not read the ledger at all. **The CLI's first mutating command.** `chronos.cli proposer` had been stdout-only since ADR-0023 — "a tool that mints text is not a tool that grants", pinned by a writes-nothing test. `revoke` is the first subcommand that writes anything, and what it writes is one row in the trading database; the writes-nothing pin was narrowed to `mint`/`check` rather than deleted, and both still pass under it. The direction is authority-removing only, which is why this was buildable without an owner gate — but a CLI that can now reach the trading database is disclosed here regardless. Tests: `tests/safety/test_proposer_revocation_exercised.py` (15), including a real app booted through its lifespan that accepts a credential, has it revoked mid-session, and refuses it in the same process with the same in-memory snapshot; **ten** independent conjuncts each verified by reverting them alone and confirming a distinct named failure. That exercise earned its keep three times. First: reverting the `check`-reporting conjunct passed, because the assertion looked for "UNVERIFIED" anywhere in the output and the explanatory NOTE line contains that word — a vacuous assertion of exactly the class this repository's guard-the-guard tests exist to catch; it now asserts on the registration's own line. Second, on review: the route's fail-closed read was **claimed in a comment and proven by nothing** — making `_is_revoked` swallow its exception and return `False` (an unreadable ledger silently admitting every credential) left the entire suite green. That is the R-24..R-27 defect class exactly — a documented control that could not be shown to fire — and it is now pinned by `test_an_unreadable_ledger_refuses_rather_than_admitting`, which drops the revocation table out from under a running backend and asserts 503. Two positive controls were added alongside it, because every other test here asserts a refusal and a "refuse everything once any revocation exists" bug would have satisfied all of them: a second unrevoked proposer keeps working in the same process between the same two calls, and the registry-off posture is proven untouched with a revocation row deliberately present. **Third, and the one that had actually shipped:** stacking A4 on this branch surfaced that `proposer check` no longer wrote nothing. Reading the ledger builds a `Database`, and `Database` *prepares* its target — it mkdirs the parent and `O_CREAT`s the SQLite file — so a command documented as writes-nothing was creating `data/` and an empty database in whatever directory the operator ran it from. Every test here had handed `check` an explicit `--database-url` into a `tmp_path` that already existed, so the narrowed writes-nothing pin never ran the default path an operator actually takes. `_read_revocations` now reports a configured-but-absent ledger as unreadable (`NoLedgerFile` → UNVERIFIED) instead of bringing it into existence by asking about it, and the new pin asserts on the whole directory tree rather than on named files. **Residuals (disclosed):** (a) the rest of the registry stays a boot-time snapshot — enabling or re-registering still needs a restart, deliberately, because a grant should not change under a running process; (b) revocation is permanent with no un-revoke, so an operator who revokes the wrong credential must mint a replacement and restart — chosen over an un-revoke path, since a reversible kill invites reversing it under pressure; (c) the ledger is read on every route authentication and every drain resolution, which is one extra database read per proposal on the trade path — bounded by `proposals_per_tick`, and the alternative is a cache that reintroduces the staleness this closes; (d) an unreadable ledger refuses (503 at the route, and the drain's own refusal), so a corrupt database stops proposing entirely rather than proposing unchecked — the fail-closed direction, and it means database health is now on the proposal path; (e) revocation stops *authorship*, not a compromised worker's other capabilities — a leaked proposer credential never had any, but a compromised worker process holding the general API token is a different incident that this does not address; (f) `revoke` requires the registry file in order to turn a proposer id into the credential hash it kills, so a lost or corrupted registry leaves no CLI path to revoke by raw hash — a `--secret-sha256` escape hatch was considered and deliberately not built, because a registry the backend cannot load already refuses **every** proposal with a 503 (`auth.registry is None`), so the leak is neutralized by the same condition that would block the escape hatch's use case; (g) mandate revocation is exposed as a terminal route while proposer revocation is a CLI command — the asymmetry is disclosed, not resolved, and whether the terminal should surface this too is a separate question; (h) as everywhere on this path, fixture-verified only: no real gateway has ever been connected. |
| R-52 | Policy-content pinning invites over-reading: a digest that binds a FILE could be read as proof of what the model RAN | L | MITIGATED (2026-08-14, A4) — the binding is real and its bound is stated wherever the value is produced | R-47 residual (b) said `prompt_version` was an owner-typed label with nothing digesting the policy into it, so a policy edit was unattributable unless the owner remembered to bump it by hand — and a mandate pinned to that label kept agreeing across an edit it never saw. **What A4 does:** `proposer mint --policy-file` derives the label as `sha256(policy bytes)[:16]`; `proposer fingerprint --policy-file` prints the same value so a mandate can be re-pinned without rotating a credential; `proposer check --policy-file` reports, per registration, whether the policy on this machine *still* matches what was registered (MATCHES/DIFFERS). An edited policy now stops agreeing with the mandate's pin, and admission's existing check 8 refuses `VERSION_PIN_MISMATCH` — no new refusal was added, an existing one simply stopped being fooled. Byte-sensitive over raw bytes (trailing whitespace is an edit); an empty or unreadable policy is refused rather than digested, because the worker itself refuses to start on an empty policy and minting a credential that claims one would put a fiction in the registry; passing both `--policy-file` and `--prompt-version` is refused rather than resolved by precedence, since a label whose provenance depends on which flag won is worse than no label. All commands remain stdout-only and write nothing. **The risk this row exists for is the misreading, and it is why the bound is repeated in the module docstring, in `mint`'s own output, in `fingerprint`'s output, in `docs/model_worker.md`, and here:** the digest binds the policy file **as it was at mint time** to the registration's label. It does **not** prove which policy the worker ran — the worker reads `CHRONOS_WORKER_POLICY_FILE` at startup and nothing in Chronos observes that read. `check --policy-file` narrows this to drift detection on one machine, which is the strongest claim available without the worker attesting its own digest. Tests: `tests/safety/test_policy_pinning_exercised.py` (13), including both drift verdicts fired on real bytes — a reporting control that never produces its negative verdict is the R-25..R-27 shape in a different costume — and **six** conjuncts each verified by reverting them alone and confirming a distinct named failure (the derivation itself, the both-flags refusal, the empty-policy refusal, the unreadable-policy refusal, the drift verdict, and the digest being over raw bytes rather than stripped text). The thirteenth test exists only because this item was stacked on A3: `check` now carries both A3's REVOKED state and A4's policy verdict for the same registration, an interaction neither branch could have written alone, and it asserts both survive — a merge that quietly drops one feature's output is the failure this repository would otherwise find in production. **Residuals (disclosed):** (a) worker attestation is absent, as above, and closing it is a worker change, not a CLI one; (b) the label is truncated to 64 bits — ample against accident, and it is a label rather than a security boundary since nothing authenticates on it, but it is not a collision-resistant commitment; (c) pinning is opt-in: mint without `--policy-file` and the old typed-label behavior is unchanged, deliberately, so this adds an option rather than imposing one; (d) `check --policy-file` compares against whatever file the operator names, so it verifies the machine it runs on, not the machine the worker runs on. |
| R-53 | The account allowlist — a load-bearing R-02 control — could not be configured through any documented syntax | M | MITIGATED (2026-08-14) | **The defect.** `pydantic-settings` JSON-decodes a field whose type is complex (a tuple) *before* validators run, so `SymbolAllowlist`'s `BeforeValidator` string branch was unreachable from the environment and from `.env`. Every documented value of `IB_ACCOUNT_ALLOWLIST`, `CRYPTO_ALLOWLIST` and `SYMBOL_ALLOWLIST` raised `SettingsError` at import — the bare `IB_ACCOUNT_ALLOWLIST=` and the comma form the example ships, and the `SYMBOL_ALLOWLIST=AAPL,MSFT,SPY` line in `.env.example` itself. Only a JSON array ever worked and nothing said so, so **`cp .env.example .env` — the README's own setup step — could not boot**. **Why it survived.** Every allowlist test built `Settings(...)` directly, which bypasses the settings sources and hands the validator a real string; the suite exercised a path production never takes. That is the R-24..R-27 shape applied to configuration parsing rather than to a gate, and it is the second instance found in this PR (the first being `GET /terminal/bars`, R-50). **Direction of failure.** Fail-closed throughout: the process refused to start rather than admitting a wrong or empty allowlist, so no order path was ever widened by it. The cost was that the *safe* value — an empty allowlist, meaning deny — was unrepresentable, so an owner standing the system down by blanking the list got a crash instead of the deny posture they asked for. **Fix.** `NoDecode` on the annotation so the raw string reaches the parser, which now accepts the comma form (as documented) and a JSON array (as previously worked, kept so no working configuration silently changes meaning); a malformed value opening with `[` refuses loudly rather than falling through to a comma split that would produce one garbage symbol. Semantics unchanged in every direction: empty is still deny, and the duplicate, alphanumeric, account-match and full ADR-0009 conjunction checks all still fire on the parsed result — pinned by test. **Note this is a behavior change the owner is approving by merging:** a configuration that previously crashed the process now loads. That is the point of the fix, and it makes previously-unusable live configuration usable — still behind every existing gate, and still requiring the whole ADR-0009 conjunction. **Residual:** the same pre-decode behavior applies to any future complex-typed setting; a new tuple/list field without `NoDecode` will reintroduce this class. Tests: `tests/unit/test_settings.py::TestAllowlistsParseFromTheEnvironment` (6, driven through the environment and a real `.env` file, including the shipped `.env.example`), 5/5 conjuncts verified by reverting each alone. |
| R-54 | The repository's shared history was structurally unguarded: an unprotected default branch, divergence computed against stale bases, and branches nothing cleans up | M | MITIGATED (2026-08-22, D-33) — settings enforce the enforceable; the protocol's adoption is unproven | The failure class showed all three faces at once. Evidence: PR #82 opened against a 98-commit-stale `main` claiming "48 patch-unique commits" whose true count against the real default was zero (divergence computed against the wrong base); a host-wide sync re-pushed two SUPERSEDED branch tips as `-mickey-20260822` archive branches, manufacturing clutter and red CI out of already-merged work; and the allowlist fix stranded on `claude/chronos-a1-plan-annotation` — correct, committed, and on no path to the default branch until this package folded it in. Until 2026-08-22 nothing prevented any of it: the default was force-pushable, deletable, and directly pushable by every seat (all seats push as one account, so no actor-level distinction was even possible). **Mitigations:** the `main-integrity` ruleset (PR-only, required green `quality` check, no force-push, no deletion, no bypass actors); `delete_branch_on_merge`, so merged branches stop accumulating as re-pushable stale tips; the `refs/preserve/<seat>/<name>` convention, so preservation stops masquerading as live branches or PRs; and `docs/AGENT_PROTOCOL.md` §2/§3 — derive the default by command, compute `ahead_by` against it, `ahead_by=0` means nothing to preserve. **MITIGATED ≠ CLOSED, and the residual is specific:** the ruleset binds mechanically, but the protocol binds only seats that read it — its adoption by the non-claude seats (codex, grok, kimi, cursor, opencode) is unproven until their next PRs arrive preflighted, correctly based, and footered per the spec. Watch exactly that. |
| R-55 | A dormant PAPER management engine could be mistaken for protection, or duplicate/miss an exit when retry identity is wrong | H | MITIGATED (2026-08-25, ADR-0035/ADR-0036) — ships inert; activation is explicitly blocked | `chronos.supervisor.position_management` can persist an actual-fill-derived QQQ plan, evaluate observations, and emit proposal-only risk reductions, but it cannot place or maintain a protective order. Its sole production-tree importer is the default-off `position_admission` seam; structural tests prove no runtime module imports admission, neither module imports a concrete broker/order/transmit path, and every directive says `execution_authority="none"`. It defines no local mandate or owner-grant object; authority remains exclusively in the existing ADR-0016 supervisor. Replay is hash-checked and semantically recomputed; pending and ambiguous directives block, actual fills control breakeven, broker execution IDs are one-use, and observation/broker quantity mismatch refuses. ADR-0036 code-mitigates the former opening-registration gaps: the canonical broker port now authenticates the opening account/reconciliation/executions/aggregate position, and schema v11 enforces one opening order per deterministic managed stream. **Residuals that block activation:** (a) the existing proposal queue's economic fingerprint intentionally ignores evidence identity, so two distinct trusted management retries can collide; a dedicated authenticated management-event seam is required, not a model nonce bypass; (b) ongoing management observations and directive outcomes remain typed caller attestations—the admission seam authenticates only opening registration; (c) no scheduler or runtime import exists; (d) observing a stop/target and then proposing an order is not equivalent to a broker-held protective bracket/resting order, so disconnect, process death, gap, and queue latency remain unmeasured; (e) no real PAPER lifecycle has run. Tests: `tests/safety/test_paper_position_management.py`, `tests/safety/test_managed_position_admission.py`. |
| R-56 | Filled-position admission can misbind incomplete or stale IBKR evidence, or be over-read as broker protection | H | MITIGATED (2026-08-25, ADR-0036) — fail-closed and default-off; real PAPER proof absent | Admission requires two stable canonical-broker reads, unchanged reconciliation session/generation/timestamp, a connected bound PAPER managed account, positive executions matching Chronos `orderRef`, local broker/permanent identity, protected buy limit and cumulative terminal fill, exact QQQ contract/side/time, no competing QQQ order, and exact aggregate position equal to existing managed remainder plus the candidate fill. Open-order absence never proves fill. Repeating multi-fill VWAPs and derived CVaR values use conservative 1e-8 persistence rounding. Persisted D-48 entry risk must carry the named passing management check; schema v11 uniquely binds account+opening order, account+position, and account+permanent ID, and binding plus hash-chain registration commit atomically. Registration conflicts are typed and roll the flushed binding back. Exact replay performs no second broker read. It accepts no caller economic input and has no broker mutation or runtime consumer. **Residuals:** (a) IB Gateway execution history is bounded, so missing history produces refusal rather than recovery; (b) two stable reads cannot exclude a change immediately after the second snapshot; (c) account/contract aggregate positions cannot explain manual/external lot basis—any unexplained quantity refuses; (d) PAPER fills and some orders are simulated and do not establish LIVE equivalence; (e) ongoing management observations/outcomes, broker-held protection, scheduling, restart/ambiguous-send drills, and real PAPER evidence remain absent. Tests: `tests/safety/test_managed_position_admission.py`, `tests/integration/test_order_pipeline.py`, `tests/integration/test_migrations.py`. |
| R-57 | A static QQQ readiness report could be mistaken for evidence or silently blend incompatible data identities | H | MITIGATED (2026-08-25, D-51/ADR-0037) — authentication-only; every campaign gate remains blocked | `chronos.research.qqq_campaign_readiness` authenticates the exact readiness bytes, five referenced repository artifacts, both inherited compilers' exact blocker-code sequences, and their zero-authority posture. Its typed ledger distinguishes owner input, Chronos work, unavailable shorts, and deferred PAPER activation; it explicitly forbids identity transfer between the six-symbol QQQ release and seven-symbol base Five-Tool intake. Direct-import AST and fresh-process guards exclude registry, holdout, broker, order, persistence, execution, supervisor, service, network, and database authority dependencies. The inherited QQQ compilers load existing Five-Tool market-data types and certified-reader code; this operation neither invokes nor exposes a market-data read, and the guards do not claim otherwise. One-byte mutations of all five artifacts, authority escalation in either child compiler, blocker deletion or duplication in either child compiler, and readiness-spec authority drift all refuse. **Residuals:** this is repository identity, not data/provider truth; it cannot observe IBKR completeness, corporate-action correctness, owner merge context, power, parity, edge, PAPER behavior, or broker protection. Exact source hashes intentionally require a new reviewed readiness identity after material change. No credential, data byte, holdout, trial, gateway, account, order, PAPER runtime, funding, or promotion was accessed. Tests: `tests/safety/test_qqq_campaign_readiness.py`. |
